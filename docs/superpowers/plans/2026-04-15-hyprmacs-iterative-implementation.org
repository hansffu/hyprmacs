#+title: hyprmacs Iterative Implementation Plan
#+date: 2026-04-15
#+startup: overview
#+seq_todo: TODO(t) NEXT(n) WAIT(w@/!) BLOCKED(b@/!) DONE(d!) CANCELLED(c@)

* Goal
Build a Hyprland plugin plus Emacs package that makes one Hyprland workspace behave like an Emacs-directed workspace without replacing Hyprland's compositor, popup, or input responsibilities.

* Architecture Summary
Hyprland remains the compositor and input authority. The plugin owns workspace policy for a managed subset of tiled clients and exposes a private Unix socket IPC interface. Emacs owns buffer association, visible-set selection, and rectangle computation, then publishes snapshot-style layout updates to the plugin.

* Working Agreement
- Treat each top-level task as one reviewable increment.
- Stop at every headline marked =WAIT= and review behavior together before moving on.
- After a user approves a =WAIT= checkpoint, change that checkpoint to =DONE= immediately.
- Prefer vertical slices over large foundation-only branches.
- Keep v1 intentionally narrow so we can validate the design early.
- Keep dependencies, tooling, and runnable development environments available through Nix.
- Make a git commit when completing each top-level task unless the user explicitly says otherwise.

* Plan Maintenance
- This org file is the primary source of truth for implementation status across sessions.
- Update TODO keywords here as work progresses.
- Record decisions, scope changes, deviations, and newly discovered constraints in this file during the session in which they are discovered.
- Keep related stable reference docs under =docs/architecture/=, =docs/protocol/=, and =docs/adr/= consistent with the decisions recorded here.

* Default V1 Decisions
- One Emacs controller process and one managed workspace per active session.
- Plugin implementation in C++.
- Emacs package implementation in Emacs Lisp.
- The existing flake-based nested Hyprland environment is the base developer workflow and should be extended rather than replaced.
- IPC transport is a plugin-owned Unix domain socket under =$XDG_RUNTIME_DIR=.
- IPC payloads are newline-delimited JSON with an explicit protocol version field.
- Overlapping managed rectangles are rejected in v1 instead of guessed.
- No resize animations in v1.
- Managed workspaces use a dedicated =hyprmacs= workspace policy profile so compositor behaviors that fight Emacs layout ownership can be explicitly constrained and restored on unmanage.
- If Emacs disconnects, the plugin unmanages the workspace and returns clients to native Hyprland behavior.
- Floating windows, dialogs, popups, child frames, and posframes remain native Hyprland surfaces.

* Proposed Repository Layout
- =flake.nix=: dev shell, demo app, plugin build inputs, test commands.
- =plugin/CMakeLists.txt=: plugin and test build entrypoint.
- =plugin/include/hyprmacs/plugin_state.hpp=: core state structs shared by plugin modules.
- =plugin/include/hyprmacs/protocol.hpp=: IPC message types and translation helpers.
- =plugin/include/hyprmacs/workspace_manager.hpp=: managed workspace lifecycle.
- =plugin/include/hyprmacs/client_registry.hpp=: tracked client state and lookup indexes.
- =plugin/include/hyprmacs/client_classifier.hpp=: rules for Emacs exclusion, popup exclusion, and floating checks.
- =plugin/include/hyprmacs/ipc_server.hpp=: socket lifecycle and message dispatch.
- =plugin/include/hyprmacs/layout_applier.hpp=: visibility, geometry, and stacking application.
- =plugin/include/hyprmacs/focus_controller.hpp=: Emacs-control and client-control mode handling.
- =plugin/src/plugin_main.cpp=: Hyprland plugin entrypoints and hook registration.
- =plugin/src/workspace_manager.cpp=: workspace adoption, unmanage, and sync orchestration.
- =plugin/src/client_registry.cpp=: managed-client registry implementation.
- =plugin/src/client_classifier.cpp=: client eligibility and exclusion checks.
- =plugin/src/ipc_server.cpp=: socket accept loop and message routing.
- =plugin/src/layout_applier.cpp=: show, hide, move, and stack operations.
- =plugin/src/focus_controller.cpp=: focus target selection and mode transitions.
- =plugin/tests/...=: pure C++ tests for protocol parsing, registry logic, and layout rules.
- =emacs/hyprmacs.el=: package entrypoint and user-facing commands.
- =emacs/hyprmacs-session.el=: session connect, disconnect, and workspace state.
- =emacs/hyprmacs-ipc.el=: JSON encoding, socket process, and message dispatch.
- =emacs/hyprmacs-buffers.el=: buffer and client association lifecycle.
- =emacs/hyprmacs-layout.el=: visible buffer detection and rectangle computation.
- =emacs/hyprmacs-mode.el=: Emacs-control and client-control commands.
- =tests/emacs/...=: ERT coverage for protocol, state sync, and layout computation.
- =docs/architecture/v1-scope.org=: frozen v1 boundary document.
- =docs/protocol/ipc-v1.org=: protocol contract and example payloads.
- =docs/adr/...=: focused architectural decisions discovered during implementation.
- =docs/testing/manual-smoke-tests.org=: nested Hyprland manual validation checklist.

* Iterative Plan
** DONE Task 0: Lock the prototype contract
Purpose:
- Freeze the first implementation target before code exists.

Files:
- Create =docs/architecture/v1-scope.org=
- Create =docs/protocol/ipc-v1.org=
- Create =docs/adr/0001-v1-boundaries.org=

Steps:
*** DONE Write the v1 scope document
- Explicitly include: one managed workspace, native floating windows, native dialogs and popups, native Emacs child frames, single-controller IPC, and non-overlapping rectangles.
- Explicitly exclude: multi-workspace management, prefix mode, animations, independent plugin layout persistence, and overlap support.
*** DONE Define protocol version 1
- Specify message envelope fields: =version=, =type=, =workspace_id=, =timestamp=, and payload object.
- Fully define the first coarse message set: =manage-workspace=, =unmanage-workspace=, =request-state=, =workspace-managed=, =workspace-unmanaged=, =state-dump=.
- Reserve but do not implement the later layout fields now: =selected_client=, =input_mode=, =visible_clients=, =hidden_clients=, =rectangles=, =stacking_order=.
*** DONE Write the first architecture decision record
- Record why JSON over Unix socket is acceptable for v1.
- Record why overlap is rejected in v1.
- Record why disconnect restores native workspace behavior.
*** DONE Review task 0 with user
- Confirm the v1 boundaries before any code scaffold is created.
*** DONE Session note: branch and workflow decision
- By user request, this session proceeds without a git worktree.
- Implementation work continues on local branch =task-0-prototype-contract= instead of =main=.
*** DONE Session note: nix environment requirement
- Required dependencies, build tools, and runnable developer workflows must be supported through Nix.
- The existing flake that boots a nested Hyprland session is the base environment to extend in later tasks.
*** DONE Session note: task commit rule
- Completed top-level tasks should be committed before moving on unless the user explicitly overrides that rule.

** DONE Task 1: Bootstrap the repository and developer loop
Purpose:
- Make the repo buildable and testable before adding behavior.

Files:
- Modify =flake.nix=
- Create =plugin/CMakeLists.txt=
- Create =plugin/src/plugin_main.cpp=
- Create =plugin/tests/CMakeLists.txt=
- Create =emacs/hyprmacs.el=
- Create =tests/emacs/bootstrap-tests.el=
- Create =docs/testing/manual-smoke-tests.org=

Steps:
*** DONE Add plugin build scaffolding
- Create a minimal shared-library target that matches Hyprland's plugin loading expectations.
- Add a trivial plugin entrypoint that logs successful load and unload.
*** DONE Add Emacs package scaffolding
- Create a package entry file with a version constant, feature provide, and placeholder interactive commands for connect and manage-workspace.
*** DONE Add test entrypoints
- Configure =ctest= for plugin-side pure tests.
- Configure =ERT= batch execution for Emacs tests.
*** DONE Extend the Nix dev environment
- Add build tooling needed for C++, CMake, and test execution.
- Extend the existing flake-based nested Hyprland session instead of creating a parallel non-Nix workflow.
- Add a demo app or script that starts nested Hyprland with the plugin path wired into the config.
*** DONE Session note: flake tool list split
- The flake should separate =commonTools=, =agentTools=, and =devTools= to keep sandbox-visible tools and the developer shell aligned without duplicating package lists.
*** DONE Session note: sandbox flake target
- Expose =codex-sandbox= as both a flake package and app so restart flow can use =nix run .#codex-sandbox= directly.
*** DONE Session note: sandbox artifact ignore
- Ignore the empty =.codex= file created by the sandbox wrapper so it does not pollute repository status.
*** DONE Session note: nix execution boundary
- Keep the flake updated in-repo, but let the user run =nix= commands outside the agent sandbox for validation and restart flows.
*** DONE Session note: local bootstrap verification
- Local sandbox verification now covers =cmake= configure, =cmake --build=, =ctest=, ERT bootstrap tests, and byte-compilation of =emacs/hyprmacs.el=.
- The remaining authoritative Task 1 check is loading the built plugin in the user's nested Hyprland session.
*** DONE Session note: justfile bootstrap commands
- Added a top-level =justfile= with =just build=, =just run=, and =just load= recipes for the Task 1 bootstrap workflow.
*** DONE Session note: guard against fallback plugin load
- =just load= now refuses to load a plugin artifact that contains fallback markers (missing real Hyprland headers at build time) and tells the user to rebuild first.
*** DONE Session note: nix-native plugin build flow
- Task 1 bootstrap flow now uses =nix build .#hyprmacs-plugin= for plugin artifacts, with =just load= consuming the resulting =result/lib/*.so= output.
*** DONE Session note: hyprmacs-load run path
- Simplified runtime testing flow: =just run= maps to =nix run .=, and =just load= maps to =nix run .#hyprmacs-load= where the loader script resolves and loads the plugin from the Nix store.
*** DONE Session note: nested config autoload and quieter runtime
- Nested Hyprland now autoloads the plugin via =plugin = ${hyprmacsPlugin}/lib/hyprmacs.so= in the generated config, so =just run= starts with hyprmacs loaded.
- Kept =just load= (=nix run .#hyprmacs-load=) as a fallback/manual reload path.
- Switched nested runtime package from =hyprland-debug= to =hyprland= to avoid debug/profiling noise during normal =hyprctl= usage.
*** DONE Session note: mkHyprlandPlugin meta requirement
- The plugin derivation must include a =meta= attribute (description/license/platforms) because =hyprlandPlugins.mkHyprlandPlugin= merges =args.meta= during evaluation.
*** DONE Session note: plugin derivation source root
- The Nix plugin derivation must use =src = ../plugin= so CMake sees the plugin =CMakeLists.txt= at source root.
*** DONE Session note: nixpkgs channel selection
- Switched flake input from =nixos-unstable= to =nixpkgs-unstable= for general nixpkgs tracking.
*** DONE Session note: bootstrap hash mismatch handling
- During Task 1 bootstrap, Hyprland API hash mismatch in =PLUGIN_INIT= is treated as a warning notification instead of a fatal throw so the empty plugin can load for smoke validation.
*** DONE Review task 1 with user
- Stop when the empty plugin loads and the empty Emacs package byte-compiles.

Validation:
- =nix develop=
- =cmake -S plugin -B build/plugin=
- =cmake --build build/plugin=
- =ctest --test-dir build/plugin --output-on-failure=
- =emacs -Q --batch -L emacs -L tests/emacs -l bootstrap-tests.el -f ert-run-tests-batch-and-exit=

** DONE Task 2: Implement shared protocol and state models first
Purpose:
- Put the hardest coordination contract under test before compositor behavior enters the picture.

Files:
- Create =plugin/include/hyprmacs/plugin_state.hpp=
- Create =plugin/include/hyprmacs/protocol.hpp=
- Create =plugin/src/protocol.cpp=
- Create =plugin/tests/protocol_tests.cpp=
- Create =emacs/hyprmacs-ipc.el=
- Create =emacs/hyprmacs-session.el=
- Create =tests/emacs/protocol-tests.el=

Steps:
*** DONE Define canonical identifiers
- Standardize how workspace IDs, client IDs, buffer IDs, and mode names are represented on both sides.
*** DONE Implement protocol encode and decode helpers
- Build parser and serializer helpers on the plugin side.
- Build parser and serializer helpers on the Emacs side.
*** DONE Add golden tests for wire compatibility
- Add C++ tests for decoding example JSON payloads.
- Add ERT tests that encode the same objects and compare them to the documented payload shapes.
*** DONE Add a fake transport harness
- Make it possible to drive the Emacs session code with fixture payloads before a real plugin server exists.
*** DONE Review task 2 with user
- Confirm the message contract before event hooks or layout logic are added.

*** DONE Session note: canonical identifier and mode representation
- Workspace IDs, client IDs, and buffer IDs are represented as opaque strings on both sides.
- Input mode is represented as =emacs-control= and =client-control= on the wire, and mapped to Emacs symbols =emacs-control= and =client-control= in Lisp.

*** DONE Session note: protocol helper coverage in Task 2
- Plugin side includes explicit parse and serialize helpers for frame envelopes and =state-dump= payloads.
- Emacs side includes envelope construction, newline-delimited frame encode/decode, mode conversion helpers, and frame splitting for incremental reads.

*** DONE Session note: fake transport scope
- Task 2 fake transport is in-memory only: outbound frames are captured in an outbox and inbound fixture frames are applied through session handlers.
- Real Unix socket lifecycle and controller arbitration remain in Task 5.

Validation:
- Run plugin protocol tests and ERT protocol tests with fixture payloads from =docs/protocol/ipc-v1.org=.

** DONE Task 3: Build the plugin event tap and managed-state registry
Purpose:
- Give the plugin read-only awareness of relevant Hyprland state before it starts changing window behavior.

Files:
- Create =plugin/include/hyprmacs/workspace_manager.hpp=
- Create =plugin/include/hyprmacs/client_registry.hpp=
- Create =plugin/include/hyprmacs/client_classifier.hpp=
- Create =plugin/src/workspace_manager.cpp=
- Create =plugin/src/client_registry.cpp=
- Create =plugin/src/client_classifier.cpp=
- Create =plugin/tests/client_registry_tests.cpp=

Steps:
*** DONE Subscribe to Hyprland lifecycle hooks
- Track client open, close, workspace move, focus change, title change, and float-state change events.
*** DONE Review task 3.1 with user
- Validate the event tap can connect to Hyprland's event socket and log tracked lifecycle events in nested Hyprland.
- Do not start Task 3.2 until this checkpoint is approved.
*** DONE Implement the in-memory registry
- Store client identity, workspace identity, visibility state, selected state, rectangle, eligibility, and association placeholders.
*** DONE Implement client classification rules
- Exclude Emacs itself.
- Exclude floating clients and obvious popup or transient clients.
- Mark later edge cases with explicit TODO comments in code only if backed by an ADR or issue.
*** DONE Add a plugin state dump path
- Expose a coarse debug command or log path that prints current managed and eligible client state without mutating it.
*** DONE Review task 3 with user
- Stop when we can inspect tracked clients in nested Hyprland without any layout control yet.

*** DONE Session note: Task 3.1 event subscription mechanism
- Task 3.1 uses Hyprland's event socket at =$XDG_RUNTIME_DIR/hypr/$HYPRLAND_INSTANCE_SIGNATURE/.socket2.sock= for lifecycle event intake in the plugin.
- Tracked event names in this step: =openwindow=, =closewindow=, =movewindow=, =activewindow=, =windowtitle=, and =changefloatingmode=.
- Event parsing and counting are isolated in =workspace_manager= and covered by a dedicated unit test target.
- Floating-mode tracking was broadened to accept versioned/variant names (including =changefloatingmodev2= and any event name containing =floating=) after manual validation showed title/focus events but no floating event with the initial exact-name filter.
- Temporary untracked-event logging was used for diagnosis and then reverted after confirming this runtime does not emit a float-change event on =.socket2.sock= during =togglefloating=.

*** DONE Session note: local non-Nix build and load path
- Added top-level =Makefile= with =make build=, =make test=, and =make load= for local plugin workflows without =nix build=.
- Keep =justfile= on user-facing Nix commands (=just build=, =just run=, =just load=) while the =Makefile= exists as an agent/developer local path.

*** DONE Session note: Task 3 registry and classification wiring
- Added =ClientRegistry= state tracking for client identity, workspace, title, app ID, visibility, selected state, floating flag, eligibility, managed placeholder, rectangle placeholder, and buffer association placeholder.
- Event ingestion now uses v2 events where available (=openwindowv2=, =activewindowv2=, =windowtitlev2=, =movewindowv2=) to maintain richer per-client state.
- Added coarse classification rules that currently exclude Emacs clients, known popup/transient-like clients, and floating clients from eligibility.

*** DONE Session note: Task 3 state-dump logging
- The plugin now emits =state-dump= log lines after registry mutations, including global counts and per-client snapshots, so Task 3 can be validated without IPC or layout control.

Validation:
- Launch nested Hyprland, open a few windows, and confirm the registry reflects open and close events plus float transitions.

** DONE Task 4: Build the Emacs-side session and buffer skeleton
Purpose:
- Let Emacs observe plugin state and create the logical handles users will interact with later.

Files:
- Modify =emacs/hyprmacs.el=
- Create =emacs/hyprmacs-buffers.el=
- Modify =emacs/hyprmacs-session.el=
- Create =tests/emacs/session-tests.el=
- Create =tests/emacs/buffer-tests.el=

Steps:
*** DONE Create the session state object
- Track current connection status, active workspace, known clients, associated buffers, selected client, and current input mode.
*** DONE Create user-visible commands
- Add interactive commands for connect, disconnect, manage current workspace, unmanage workspace, and dump known state into an Emacs buffer.
*** DONE Create buffer association primitives
- Define the buffer-local metadata that identifies a managed client buffer.
- Add helper functions to create, rename, and clean up those buffers from plugin events.
*** DONE Drive the Emacs code from fake plugin payloads
- Use fixture messages to prove state transitions happen correctly before the real socket is connected.
*** DONE Review task 4 with user
- Confirm the Emacs-side UX for state inspection before real IPC is enabled.

*** DONE Session note: Task 4 Emacs modules and tests
- Added =emacs/hyprmacs-buffers.el= for client-to-buffer mapping, title sync, and cleanup helpers.
- Expanded =emacs/hyprmacs-session.el= with explicit connect/disconnect transitions, session fields for known clients and associations, and fake-transport command paths.
- Expanded =emacs/hyprmacs.el= user commands to call session primitives and render a =*hyprmacs-state*= inspection buffer.
- Added ERT coverage in =tests/emacs/session-tests.el= and =tests/emacs/buffer-tests.el= for command-side effects and buffer lifecycle behavior.

*** DONE Session note: stale bytecode handling in Task 4 tests
- Set =load-prefer-newer= in Task 4 session tests so source edits are always preferred over stale =.elc= artifacts during batch runs.

*** DONE Session note: just emacs entrypoint
- Updated =just emacs= to load =hyprmacs= (package entrypoint) instead of =hyprmacs-session= so interactive commands are available immediately.
- Updated =just emacs= to delete stale =emacs/*.elc= before launching Emacs to avoid loading stale bytecode during manual testing.
- Added =just emacs-test= to run an automated non-GUI smoke flow and write deterministic debug output to =logs.txt=.
- User requested reverting from GUI smoke to the original batch/non-GUI flow because GUI variant provided less useful debug signal.

Validation:
- Run ERT tests for session transitions and buffer creation.
- Open the state buffer in Emacs and inspect mock client data rendered there.

** DONE Task 5: Wire up the real IPC handshake and coarse workspace commands
Purpose:
- Replace the fake transport with a real plugin-owned socket and prove that both sides stay synchronized.

Files:
- Create =plugin/include/hyprmacs/ipc_server.hpp=
- Create =plugin/src/ipc_server.cpp=
- Modify =plugin/src/plugin_main.cpp=
- Modify =plugin/src/workspace_manager.cpp=
- Modify =emacs/hyprmacs-ipc.el=
- Modify =emacs/hyprmacs-session.el=
- Create =plugin/tests/ipc_server_tests.cpp=

Steps:
*** DONE Start the plugin socket server
- Create and own a Unix socket path under =$XDG_RUNTIME_DIR=.
- Support exactly one controller connection in v1 and reject extra controllers with a clear error.
*** DONE Implement the first live command path
- Handle =manage-workspace=, =unmanage-workspace=, and =request-state= on the plugin side.
- Send =workspace-managed=, =workspace-unmanaged=, and =state-dump= back to Emacs.
*** DONE Implement reconnect-safe session handling
- Ensure Emacs can disconnect and reconnect without leaking duplicate event handlers or stale process objects.
*** DONE Add integration logging
- Log every message type at debug level with enough context to diagnose sync bugs.
*** DONE Review task 5 with user
- Stop when Emacs can manage and unmanage a workspace and display real plugin state.

*** DONE Session note: Task 5 plugin socket path and command routing
- Added plugin-owned Unix socket server at =$XDG_RUNTIME_DIR/hyprmacs-v1.sock= with newline-delimited JSON frames.
- Added routing for =manage-workspace=, =unmanage-workspace=, and =request-state= to corresponding plugin responses.
- Added =ipc_server_tests= coverage for command routing response shapes.

*** DONE Session note: Task 5 reconnect-safe Emacs transport
- Emacs session now connects through a real local socket process, with explicit filter/sentinel handling and partial-frame buffering.
- Connect now tears down any existing process first; disconnect clears process handles and transport functions.

*** DONE Session note: Task 5 integration logging
- Plugin now logs IPC listen path, controller connect/disconnect, every received message type, and every sent message type.

Validation:
- Start nested Hyprland, connect Emacs, manage a workspace, request state, and verify the state buffer matches the live workspace contents.

** DONE Task 6: Prove the hidden-client mechanism in isolation
Purpose:
- Retire the highest-risk compositor behavior before layout work depends on it.

Files:
- Create =docs/adr/0002-hidden-client-strategy.org=
- Create =plugin/include/hyprmacs/layout_applier.hpp=
- Create =plugin/src/layout_applier.cpp=
- Create =plugin/tests/layout_applier_tests.cpp=
- Modify =docs/testing/manual-smoke-tests.org=

Steps:
*** DONE Implement a minimal show and hide experiment path
- Add temporary plugin-only commands that can hide a tracked tiled client and then restore it without destroying the application.
*** DONE Evaluate the mechanism against real applications
- Test at least =foot= and one GUI app inside nested Hyprland.
- Verify the client stays alive, disappears from the visible tiled layout, and returns intact.
*** DONE Write the strategy ADR
- Document the exact Hyprland primitive used for hide and restore.
- Document the failure cases discovered during the spike.
*** DONE Review task 6 with user
- Do not proceed to the first vertical slice until we trust the hide mechanism.

*** DONE Session note: Task 6 hide and restore mechanism
- Added =LayoutApplier= with hide/restore bookkeeping by client ID and original workspace.
- Hide command path uses =hyprctl dispatch movetoworkspacesilent special:hyprmacs-hidden,address:<client-id>=.
- Restore command path uses =hyprctl dispatch movetoworkspacesilent <workspace>,address:<client-id>=.

*** DONE Session note: Task 6 temporary control surface
- Added temporary plugin IPC commands =debug-hide-client= and =debug-show-client= plus result frames for manual validation.
- Added temporary Emacs commands =hyprmacs-debug-hide-client= and =hyprmacs-debug-show-client= to drive the spike without raw socket tooling.

*** DONE Session note: Task 6 docs sync
- Added =docs/adr/0002-hidden-client-strategy.org= and updated =docs/testing/manual-smoke-tests.org= with hide/restore scenarios.
- Added a Task 6 temporary command section to =docs/protocol/ipc-v1.org= to avoid protocol drift during the spike.

*** DONE Session note: Task 6 dispatch path hardening
- Replaced hide/restore execution from shelling out to =hyprctl= with direct writes to Hyprland command socket =.socket.sock=.
- Added extra runtime error logging around connect/send/recv and added newline-terminated dispatch writes to reduce protocol ambiguity.

*** DONE Session note: Task 6 user validation outcome
- User validated restore behavior in nested Hyprland after dispatch hardening ("Now it worked to show!").
- Task 6 review checkpoint approved and closed.

Validation:
- Manual smoke test passes for hide, restore, workspace switch away and back, and client close while hidden.

** DONE Task 7: Deliver the first vertical slice with one visible managed client
Purpose:
- Make the workspace feel meaningfully different as early as possible.

Files:
- Modify =plugin/src/workspace_manager.cpp=
- Modify =plugin/src/layout_applier.cpp=
- Create =plugin/include/hyprmacs/focus_controller.hpp=
- Create =plugin/src/focus_controller.cpp=
- Modify =emacs/hyprmacs-buffers.el=
- Modify =emacs/hyprmacs-session.el=
- Create =emacs/hyprmacs-mode.el=
- Create =tests/emacs/mode-tests.el=

Steps:
*** DONE Adopt eligible clients into a managed set
- When a workspace becomes managed, adopt existing eligible tiled clients.
- Auto-adopt newly opened eligible tiled clients in that workspace.
*** DONE Create one buffer per managed client
- Ensure each adopted client gets a corresponding Emacs buffer with stable identity.
*** DONE Make exactly one managed client visible
- Tie visibility to the selected managed buffer.
- Hide every other managed client while keeping them alive.
*** DONE Implement the first two input modes
- Add Emacs-control mode where Emacs keeps focus.
- Add client-control mode where the selected managed client receives keyboard focus.
*** DONE Add the first user-facing switching commands
- Support selecting a managed buffer from Emacs and reflecting that in the workspace.
*** DONE Review task 7 with user
- Stop when buffer switching can swap one live client in and out without recreating it.

*** DONE Session note: Task 7 selection-driven single visibility
- Added =set-selected-client= IPC command path; when selection changes, plugin now hides other managed clients and keeps the selected client shown.
- Added =layout-applied= response and updated state dump flow to reflect selected client after visibility updates.

*** DONE Session note: Task 7 input mode command path
- Added =set-input-mode= command with =mode-changed= response and state-dump propagation.
- Workspace state dump now includes current input mode for managed workspaces.
- Added =FocusController= and runtime focus dispatch for mode switching: =client-control= focuses selected managed client, =emacs-control= attempts to focus Emacs client in the managed workspace.

*** DONE Session note: Task 7 adoption backfill for pre-existing windows
- Added temporary =seed-client= command so Emacs can seed already-open clients when a workspace first becomes managed.
- =hyprmacs-manage-current-workspace= now performs a one-shot =hyprctl -j clients= seed pass to avoid missing the initial terminal/emacs-launch window.
- Added automatic Hyprland instance-signature detection in Emacs command paths so smoke commands can target a running nested instance without manual =HYPRLAND_INSTANCE_SIGNATURE= export.

*** DONE Session note: Task 7 user-facing commands
- Added Emacs commands to set selected managed client and set input mode, backed by new session send helpers.
- Session handler now applies =layout-applied= and =mode-changed= frames to local session state.
- Added =hyprmacs-run-smoke-test= for scripted connect/manage/request/select/mode-switch flow with state + =hyprctl clients= capture to =logs.txt=.
- Added =hyprmacs-run-gui-smoke-test= for GUI-mode automation: spawns a second =foot= client, runs selection and mode toggles, and logs =hyprctl activewindow= snapshots after each step.
- Extended non-GUI =hyprmacs-run-smoke-test= logging to capture =hyprctl activewindow= snapshots and intermediate =after-client-control= state, so mode diagnostics are still visible after reverting to non-GUI workflow.

*** DONE Session note: Task 7 client-id canonicalization and seed false handling
- Fixed registry-level client-id canonicalization to always use =0x...= form, preventing duplicate records for the same client (raw hex vs prefixed forms).
- Fixed Emacs JSON boolean normalization for seed payloads to treat both =:json-false= and =:false= as false; this prevented incorrectly marking seeded tiled clients as floating/ineligible.
- Verified by direct socket inspection that =seed-client= now yields non-empty =eligible_clients= and =managed_clients= in subsequent =state-dump= frames.

*** DONE Session note: Task 7 managed-set adoption
- Added registry reconciliation so managed membership is derived from three facts: workspace is currently managed, client is in that workspace, and client remains eligible.
- Workspace manage/unmanage now immediately recalculates managed membership across tracked clients.
- Lifecycle updates (open, close, move, title/classification changes, floating changes) also recalculate managed membership so new eligible clients auto-adopt and ineligible/moved clients auto-drop.

*** DONE Session note: Task 7 internal hidden workspace move handling
- Identified a switching bug where hidden-client moves to internal workspace =-98= could remove that client from the managed snapshot.
- Added a regression test in =plugin/tests/workspace_manager_tests.cpp= that verifies an internal hide move event keeps both clients in managed state for workspace =1=.
- Updated plugin move-event handling to ignore internal hide targets (=-98= and =special:hyprmacs-hidden=) for registry workspace updates so hidden clients remain switchable.

*** DONE Session note: Task 7 managed buffer identity sync
- Emacs now synchronizes one buffer per managed client from each =state-dump=, using client ID as stable identity and eligible-client metadata for naming and app id.
- Session state now tracks =:associated-buffers= and prunes buffers for clients that leave the managed set.
- Added ERT coverage for create/update/remove behavior when managed client membership changes across snapshots.

Validation:
- Manage a workspace with multiple apps.
- Switch managed buffers in Emacs and verify only the selected client is visible.
- Toggle Emacs-control and client-control modes and confirm focus moves as expected.

** DONE Task 8: Implement rectangle-first layout updates and multi-client visibility
Purpose:
- Upgrade from a one-client prototype to the core design model.

Files:
- Create =emacs/hyprmacs-layout.el=
- Modify =emacs/hyprmacs-buffers.el=
- Modify =emacs/hyprmacs-session.el=
- Modify =plugin/src/layout_applier.cpp=
- Modify =plugin/src/workspace_manager.cpp=
- Create =tests/emacs/layout-tests.el=
- Modify =plugin/tests/layout_applier_tests.cpp=

Steps:
*** DONE Compute managed-client rectangles in Emacs
- Detect which managed buffers are visible in the current Emacs window tree.
- Convert visible window geometry into plugin-facing rectangles relative to the Emacs frame.
*** DONE Send snapshot-style =set-layout= messages
- Include workspace ID, selected client, input mode, visible clients, hidden clients, rectangles, and stacking order.
*** DONE Apply snapshot layouts in the plugin
- Show clients listed as visible.
- Hide clients listed as hidden.
- Move visible clients into the exact rectangles specified by Emacs.
*** DONE Reject unsupported layouts explicitly
- Detect overlapping rectangles in Emacs before sending them.
- Validate the same constraint in the plugin and log a protocol error if violated.
*** DONE Review task 8 with user
- Stop when simple Emacs splits map to two or more live managed clients.

*** DONE Session note: Task 8 rectangle-first Emacs layout module
- Added =emacs/hyprmacs-layout.el= with window-tree rectangle extraction for managed buffers, snapshot payload construction, and overlap/size validation before send.
- Added =hyprmacs-sync-layout= plus automatic layout sync hooks from =window-configuration-change-hook= (enabled on manage-workspace).
- Added =tests/emacs/layout-tests.el= covering rectangle extraction, payload construction, and overlap rejection.

*** DONE Session note: Task 8 set-layout protocol and plugin application path
- Added plugin =set-layout= command routing with payload parsing for =selected_client=, =input_mode=, =visible_clients=, =hidden_clients=, =rectangles=, and =stacking_order=.
- Added snapshot application in =LayoutApplier=: idempotent show/hide, move/resize dispatches, and overlap rejection.
- Added/extended plugin tests in =layout_applier_tests= and =ipc_server_tests= for success and overlap-failure behavior.

*** DONE Session note: Task 8 geometry mode bootstrap for tiled clients
- Manual validation showed pixel move/resize dispatches were accepted but tiled clients did not move.
- Added a Task 8 prototype geometry mode step that toggles a managed client into geometry-addressable mode before pixel move/resize on first visible layout application.
- This is an implementation bootstrap for Task 8 validation and will be refined in later tasks to preserve the desired managed-layer semantics separate from native floating behavior.

*** DONE Session note: Task 8 developer-loop verification updates
- Added Emacs layout tests to default test workflows (=Makefile test= and =just test=).
- Hardened fallback plugin API compile gating to require both Hyprland plugin API headers and Hyprgraphics headers before enabling real-plugin compile path.

*** DONE Session note: Task 8 live-debug findings and initial publish fix
- Direct live IPC validation on a clean nested session confirms =set-layout= can return =layout-applied ok=true= and mutate client geometry (workspace, position, size) when payload fields are complete.
- Added immediate =request-state= on =hyprmacs-manage-current-workspace= and scheduled an initial delayed =hyprmacs-sync-layout= (interactive sessions) to reduce timing gaps where managed buffers are visible but no initial layout snapshot has been published yet.
- During debugging, multiple stale nested Hyprland instances and stale controller connections to the shared socket path created misleading no-op behavior; manual validation should prefer a clean single nested session for deterministic results.

*** DONE Session note: Task 8 per-instance IPC socket path
- Root-cause for repeated \"snapshot published but wrong windows moved\" behavior was cross-instance IPC path collision: multiple nested Hyprland instances were sharing =/run/user/$UID/hyprmacs-v1.sock=.
- Updated plugin default socket bind path to instance-specific location =/run/user/$UID/hypr/$HYPRLAND_INSTANCE_SIGNATURE/hyprmacs-v1.sock=.
- Updated Emacs default socket resolution to the same per-instance path and ensured =hyprmacs-connect= sets/refreshes =HYPRLAND_INSTANCE_SIGNATURE= before connecting.
- Updated protocol transport docs to reflect per-instance socket location.

*** DONE Session note: Task 8 selected-client validity hardening
- Root-cause for layout no-op behavior: plugin =state-dump= could publish focused Emacs client IDs as =selected_client=, and later =set-layout= snapshots were rejected when that non-managed selection was echoed back.
- Updated plugin =state-dump= to include =selected_client= only when the selected client is managed in the target workspace.
- Updated =set-layout= handling to ignore (not reject) non-managed =selected_client= values so visibility/geometry updates still apply.
- Updated Emacs layout payload builder to drop =selected_client= when it is not in the current managed-client set.

*** DONE Session note: Task 8 rectangle coordinate space fix
- Manual validation showed managed clients were being moved but aligned to monitor origin instead of the Emacs frame region.
- Root-cause: Emacs was sending frame-local window rectangles while plugin geometry commands require global compositor coordinates.
- Updated Emacs layout rectangle calculation to add the frame inner origin (=frame-geometry inner-position= fallback chain) to window pixel edges before publishing =set-layout=.
- Added Emacs regression coverage to verify visible rectangle payloads include frame offset.

*** DONE Session note: Task 8 Wayland frame-origin fallback
- Further validation showed Emacs on Wayland can report frame position as =0,0=, which makes global rectangle conversion incorrect even after frame-origin offset wiring.
- Updated Emacs layout origin resolution to consult =hyprctl -j clients= and select the largest non-floating Emacs client for the current Emacs PID/workspace as the primary origin source.
- =frame-geometry= and =frame-position= remain fallback paths when Hyprland client lookup is unavailable.

*** DONE Session note: Task 8 minibuffer and Vertico auto-sync hooks
- Validation showed managed client geometry could overlap minibuffer/Vertico UI and remain stale until manual =hyprmacs-sync-layout=.
- Expanded automatic layout sync triggers beyond =window-configuration-change-hook= to also include =window-size-change-functions=, =minibuffer-setup-hook=, and =minibuffer-exit-hook=.
- Added a short debounce timer so clustered UI updates coalesce into one =set-layout= publish while still reacting quickly to minibuffer size transitions.

*** DONE Session note: Task 8 generic resize/state hook alignment
- User requested avoiding package-specific hooks and relying on generic resize-driven triggers.
- Replaced minibuffer-specific auto-sync triggers with generic =window-state-change-functions= while keeping =window-configuration-change-hook= and =window-size-change-functions=.
- Retained debounce scheduling, updated ERT coverage for hook registration/removal and schedule behavior via generic change hooks.

*** DONE Session note: Task 8 live named-daemon reproduction and set-layout null-array fix
- Reproduced in a clean nested Hyprland session using named Emacs daemon =hyprmacs-e2e= and GUI client bound to the active instance socket.
- Root-cause from live plugin logs: =set-layout= frames were rejected as \"missing required fields\" because Emacs encoded empty list fields as JSON =null= instead of =[]= (notably =hidden_clients= and potentially other array fields).
- Fixed Emacs payload builder to emit vectors for all array-like fields (=visible_clients=, =hidden_clients=, =rectangles=, =stacking_order=) so wire encoding always uses JSON arrays.
- Fixed plugin JSON string-field parsing to require a quoted string token at the field value position, preventing null values from being misparsed from later quoted tokens.

*** DONE Session note: Task 8 duplicate-rectangle guard and Emacs-side debug logging
- Added Emacs-side rectangle de-duplication by =client_id= before overlap validation and payload construction; this prevents self-overlap protocol rejects when the same managed buffer appears in multiple windows.
- Added optional per-snapshot debug logging via =hyprmacs-layout-debug-log-file= to capture emitted =set-layout= payloads during manual GUI bug hunts.

*** DONE Session note: Task 8 post-resize re-anchor in plugin geometry apply
- Live validation showed =resizewindowpixel= can shift floating managed clients vertically even when =movewindowpixel= was applied immediately before resize.
- Updated layout application to enforce =move -> resize -> move= for visible clients so top-left anchoring remains stable across minibuffer-driven height changes.
- Extended layout applier tests to assert the extra post-resize move command in snapshot application.

*** DONE Session note: Task 8 full graphical round validation and remaining prototype caveat
- Ran a full graphical validation round in nested Hyprland with named daemon workflow (=emacs --daemon=hyprmacs-e2e= + GUI client), including: frame resize changes, Vertico/minibuffer open-close cycles, selection switches, hide/restore, input-mode toggles, and managed-client close handling.
- Verified the reported minibuffer/Vertico drift path after the re-anchor fix: layout payloads and plugin commands kept =y= stable while height shrank/restored (=1368 -> 1188 -> 1368=), and managed client top-left anchors remained at expected coordinates.
- Observed a known Task 8 prototype caveat during long runs: clients moved into temporary geometry mode are marked floating and can later be reclassified as ineligible on state refresh. This is consistent with the existing Task 8 bootstrap note and remains a refinement item for later tasks.

Validation:
- Split the Emacs frame, show two managed buffers, and verify both clients become visible in matching rectangles.
- Delete one split and verify the corresponding client becomes hidden but remains alive.

** DONE Task 8A: Introduce the dedicated hyprmacs workspace policy profile
Purpose:
- Add a workspace-local policy mode that protects Emacs-directed layout semantics from conflicting Hyprland behaviors.

Files:
- Create =docs/adr/0003-hyprmacs-workspace-policy.org=
- Modify =plugin/src/workspace_manager.cpp=
- Modify =plugin/src/focus_controller.cpp=
- Modify =plugin/src/layout_applier.cpp=
- Modify =docs/testing/manual-smoke-tests.org=

Steps:
*** DONE Capture architecture and policy rules in ADR
- Document dedicated =hyprmacs= policy profile lifecycle (=manage -> enforce, unmanage/disconnect -> restore=).
- Document required baseline constraints:
  - window animations disabled for managed geometry transitions
*** DONE Implement workspace policy enforcement and restore
- Workspace manager now applies managed policy keywords on =manage-workspace=:
  - =animations:enabled = 0=
  - =misc:focus_on_activate = 0=
- Workspace manager snapshots prior option values using Hyprland =j/getoption= queries before applying policy.
- Workspace manager restores snapped policy values on =unmanage-workspace=.
- Controller disconnect transition now triggers workspace unmanage + policy restore in the manager state path.
*** DONE Add additional safeguards for layout ownership
- Prevent native focus/layout heuristics from overriding Emacs layout intent in managed mode.
- Keep managed-layer stacking and non-overlap guarantees intact.
- Added manager-level policy guard for =focus_on_activate= during managed sessions.
- Existing layout safeguards remain active:
  - plugin overlap rejection in =LayoutApplier=
  - managed-layer visibility and geometry from Emacs snapshots
  - native popup/floating surfaces remain outside managed set by classifier rules
*** DONE Review task 8A with user
- Stop when policy constraints are active and restored correctly across manage/unmanage.

*** DONE Session note: post-Task-8 architecture agreement (2026-04-17)
- User approved introducing a dedicated =hyprmacs= workspace layout/policy mode instead of relying on existing Hyprland layout defaults.
- Initial mandatory constraints for this mode:
  - window animations for managed geometry transitions must be off
- Additional constraints to implement in the same policy profile:
  - =focus_on_activate= should be constrained so managed clients do not steal keyboard ownership from Emacs-control mode
  - managed-client geometry and visibility remain authoritative from Emacs snapshots, not native tiling mutations
  - policy state should be restored cleanly when workspace is unmanaged or controller disconnects

*** DONE Session note: nested-Hyprland QoL input-mode test binds (2026-04-17)
- Updated flake nested Hyprland config to disable pointer focus stealing during manual input-mode validation:
  - =input.follow_mouse = 2=
- Updated flake nested Hyprland config to disable window animations for deterministic managed geometry tests:
  - =animations.enabled = false=
- Added nested keybind for returning keyboard focus to Emacs quickly from client-control testing:
  - =ALT+E -> focuswindow class:^(emacs)$= (native Hyprland dispatcher bind)
- Note: this keybind currently performs compositor focus handoff only; plugin-native =hyprctl dispatch hyprmacs ...= mode commands are not implemented yet and remain future work.
- Superseded by Task 13 (2026-04-19), where =ALT+E= now maps to plugin dispatcher =hyprmacs:set-emacs-control-mode=.

*** DONE Session note: Emacs command UX defaults for workspace and connection (2026-04-17)
- Interactive workspace-scoped commands now infer workspace from active Hyprland workspace by default (instead of prompting for workspace ID each invocation).
- =hyprmacs-manage-current-workspace= now auto-connects to plugin IPC when disconnected, so explicit =hyprmacs-connect= is optional for the common flow.
- Manual smoke-test docs updated to reflect the new flow.

*** DONE Session note: input-mode command UX simplification (2026-04-17)
- =M-x hyprmacs-set-input-mode= now defaults to one-shot =client-control= without prompting for mode.
- Added explicit =M-x hyprmacs-set-emacs-control-mode= command for forcing =emacs-control= from Emacs when needed.
- Kept backward-compatible non-interactive argument forms for automation/tests.

*** DONE Session note: new-client adoption UX gap discovered during manual test (2026-04-17)
- Reproduction: with a managed workspace active, launching a new app can first appear as native tiled until Emacs receives refreshed state and re-publishes layout.
- Current root cause: plugin-to-Emacs live lifecycle notifications (=client-opened/client-closed= style push) are not implemented yet; Emacs session state is primarily refreshed by explicit =request-state= command/response cycles.
- Consequence: newly opened eligible clients are managed in plugin state, but Emacs buffer/layout intent may not update immediately in the same interaction step.
- This is currently treated as not-yet-implemented behavior (synchronization gap), not a regression in existing implemented contract.

*** DONE Session note: Task 8A policy implementation and test coverage (2026-04-17)
- Injected command/query executors into =WorkspaceManager= so policy application and snapshot restore can be unit-tested without a live compositor.
- Added policy tests in =plugin/tests/workspace_manager_tests.cpp= covering:
  - apply-on-manage and restore-on-unmanage
  - restore and managed-state cleanup on controller disconnect transition.
- Plugin main now shares a raw Hyprland command-socket helper for both command dispatch and option query responses used by workspace policy handling.

*** DONE Session note: live nested focus-handoff failure diagnosis (2026-04-17)
- In live nested testing, Emacs appeared unresponsive while session state remained in =client-control=.
- Root cause observed in plugin logs: dispatch helper failed with =missing runtime env= during focus handoff and policy restore paths.
- Current plugin dispatch/query helpers read =HYPRLAND_INSTANCE_SIGNATURE= at call time; in this runtime, that env var can be unavailable later even though startup succeeded.
- Consequence: mode state may update in IPC/session state, but compositor focus handoff command can fail, leaving keyboard ownership on client surfaces.

** DONE Task 9: Harden popup, floating, and Emacs auxiliary-window behavior
Purpose:
- Make sure the design avoids the EXWM failure modes it was created to avoid.

Files:
- Create =docs/adr/0004-auxiliary-window-classification.org=
- Modify =docs/protocol/ipc-v1.org=
- Modify =docs/testing/manual-smoke-tests.org=
- Modify =plugin/src/client_classifier.cpp=
- Modify =plugin/src/client_registry.cpp=
- Modify =plugin/src/workspace_manager.cpp=
- Modify =plugin/src/ipc_server.cpp=
- Modify =plugin/src/plugin_main.cpp=
- Modify =plugin/include/hyprmacs/workspace_manager.hpp=
- Modify =plugin/include/hyprmacs/ipc_server.hpp=
- Modify =plugin/src/layout_applier.cpp=
- Modify =plugin/tests/client_registry_tests.cpp=
- Modify =plugin/tests/workspace_manager_tests.cpp=
- Modify =emacs/hyprmacs-layout.el=
- Modify =emacs/hyprmacs.el=
- Modify =tests/emacs/protocol-tests.el=

Steps:
*** DONE Tighten client exclusion rules
- Verify dialogs, transient windows, and true floating windows never enter the managed set.
*** DONE Keep Emacs-owned auxiliary windows native
- Confirm child frames and posframes are excluded even when they share process identity with the main Emacs frame.
*** DONE Validate internal Emacs layout changes
- Trigger fresh =set-layout= updates for minibuffer growth, side windows, and ordinary Emacs UI panes that change safe client geometry.
*** DONE Handle client float-state transitions
- If a managed client becomes floating, remove it from the managed set and notify Emacs.
- If a native tiled client becomes eligible again, let normal adoption rules decide whether to re-manage it.
*** DONE Review task 9 with user
- Stop when popups stay usable and Emacs auxiliary windows still render above managed clients.

*** DONE Session note: Task 9 classification and transition hardening (2026-04-18)
- Expanded popup/transient classification heuristics in =client_classifier= for chooser/portal/auth/dialog-style clients while keeping true floating clients ineligible.
- Added explicit regression coverage in =client_registry_tests= for portal chooser/auth prompt exclusion and Emacs auxiliary-window exclusion.
- =ClientRegistry::reconcile_management= now clears stale selection when selected client leaves managed membership (for example via float transition).

*** DONE Session note: Task 9 float-transition notification path (2026-04-18)
- Added managed-set transition notifications from =WorkspaceManager= on float/tile transitions:
  - =client-became-floating=
  - =client-became-tiled=
- =IpcServer= now subscribes to workspace-manager transition callbacks and emits transition frame + fresh =state-dump= to connected Emacs controller.
- Added workspace-manager regression coverage ensuring:
  - floating transition removes client from managed set and emits notify event
  - tiling transition re-adopts client and emits notify event.

*** DONE Session note: Task 9 generic Emacs layout-change trigger expansion (2026-04-18)
- Added =window-buffer-change-functions= auto-sync trigger in =hyprmacs.el= to catch side-window and visible-buffer churn through generic Emacs hooks.
- Extended Emacs protocol tests to assert hook registration/removal for the new generic trigger.

*** DONE Session note: Task 9 floating dialog validation checkpoint (2026-04-18)
- User reproduced the =About PCManFM= dialog path after restart and confirmed it remained native floating at the Task 9 review checkpoint.
- Review checkpoint closed based on this validation.

*** DONE Session note: Task 9 floating-state refresh robustness (2026-04-18)
- Added a fallback floating-state refresh path using =hyprctl j/clients= on key lifecycle events (=openwindowv2=, =activewindowv2=, =windowtitlev2=) because some nested runs did not emit reliable floating-transition events on =.socket2.sock=.
- Fixed Hyprland command-socket response handling to read full responses (not a fixed-size 256-byte buffer) so =j/clients= parsing is reliable for transition classification.
- Added workspace-manager regression coverage for open-time floating discovery and refresh-driven transition notifications when explicit floating events are absent.

Validation:
- Test pinentry or a dialog, a floating utility window, a posframe-like child frame, and a client that toggles floating state.

** DONE Task 10: Add disconnect recovery, resync, and failure handling
Purpose:
- Make the system survivable when either side lags, restarts, or disappears.
- Eliminate runtime dispatch fragility caused by environment-dependent socket resolution after startup.

Files:
- Modify =plugin/src/ipc_server.cpp=
- Modify =plugin/src/workspace_manager.cpp=
- Modify =plugin/src/layout_applier.cpp=
- Modify =plugin/src/plugin_main.cpp=
- Modify =plugin/include/hyprmacs/ipc_server.hpp=
- Modify =plugin/include/hyprmacs/workspace_manager.hpp=
- Modify =plugin/include/hyprmacs/layout_applier.hpp=
- Modify =emacs/hyprmacs-ipc.el=
- Modify =emacs/hyprmacs-session.el=
- Modify =docs/protocol/ipc-v1.org=
- Modify =docs/testing/manual-smoke-tests.org=
- Modify =plugin/tests/ipc_server_tests.cpp=
- Modify =plugin/tests/layout_applier_tests.cpp=
- Modify =tests/emacs/session-tests.el=
- Create =tests/emacs/reconnect-tests.el=
- Create =docs/adr/0005-disconnect-recovery.org=
- Modify =Makefile=
- Modify =justfile=

Steps:
*** DONE Define the authoritative cleanup path
- On Emacs disconnect, unmanage the workspace, restore managed clients to native behavior, and clear plugin-side session state.
*** DONE Add explicit resync support
- Let Emacs reconnect, request a state dump, and rebuild its session state from plugin truth.
*** DONE Harden message processing
- Handle malformed messages, unsupported versions, and stale socket paths with clear errors.
*** DONE Harden plugin dispatch/query runtime dependencies
- Remove per-call dependency on process environment for Hyprland command-socket resolution after plugin startup.
- Ensure focus handoff and policy dispatches remain functional even if =HYPRLAND_INSTANCE_SIGNATURE= is unavailable at call time.
- Add regression coverage for this path where feasible (unit tests for resolver logic and/or deterministic integration check in nested session).
*** DONE Document recovery behavior
- Write the ADR and add manual tests for restart scenarios.
*** DONE Review task 10 with user
- Stop when killing or restarting Emacs does not leave the workspace in a broken intermediate state.

*** DONE Session note: Task 10 disconnect cleanup path (2026-04-18)
- Added disconnect-time workspace restore in =IpcServer=: before controller state is cleared, plugin now calls layout cleanup to restore hidden clients and unwind temporary geometry mode for managed clients.
- Added =LayoutApplier::restore_workspace_to_native= and regression coverage to ensure hidden-client/show state and positioning-mode state do not leak across controller restarts.

*** DONE Session note: Task 10 reconnect and stale-state handling (2026-04-18)
- Emacs transport drop now clears managed snapshot fields and associated managed buffers via =hyprmacs-session--handle-transport-drop=.
- Added =hyprmacs-session-reconnect-and-resync= helper and ERT coverage in =tests/emacs/reconnect-tests.el=.
- Added stale socket path validation in =hyprmacs-ipc-resolve-socket-path= with explicit error text when the resolved socket does not exist.

*** DONE Session note: Task 10 protocol and runtime hardening (2026-04-18)
- Added explicit =protocol-error= IPC response path for malformed frames, unsupported protocol versions, and unknown command types; updated protocol docs accordingly.
- Plugin command-socket resolution now initializes and caches socket path at plugin startup, removing per-call env dependency for dispatch/query operations.
- Added regression coverage for unknown-command protocol errors and integrated reconnect tests into default =make test= / =just test= workflows.

Validation:
- Kill Emacs during both input modes, reconnect, and verify the workspace is usable and re-manageable.
- Validate mode switch handoff and policy dispatch continue working in a long-lived session where runtime env vars may be absent from plugin process environment.

** DONE Task 11: Implement explicit hyprmacs layout mode
Purpose:
- Replace dependency on native =dwindle= tiling semantics in managed workspaces with an explicit Hyprland layout mode named =hyprmacs=.

Files:
- Modify =plugin/src/workspace_manager.cpp=
- Modify =plugin/src/layout_applier.cpp=
- Modify =plugin/src/plugin_main.cpp=
- Modify =docs/adr/0003-hyprmacs-workspace-policy.org=
- Modify =docs/superpowers/plans/2026-04-15-hyprmacs-iterative-implementation.org=
- Modify =docs/testing/manual-smoke-tests.org=

Steps:
*** DONE Define the runtime contract for layout mode ownership
- Specify when managed workspaces should expose =tiledLayout= as =hyprmacs=.
- Define how layout mode is restored on unmanage/disconnect.
*** DONE Implement layout mode switching in managed lifecycle
- On =manage-workspace=, set layout mode to =hyprmacs= for that workspace.
- On =unmanage-workspace= and disconnect cleanup, restore previous layout mode.
*** DONE Guard managed geometry flow against native tiling fallback
- Ensure managed clients are not laid out by dwindle/master rules while managed.
- Keep native floating/transient behavior unchanged.
*** DONE Add verification coverage
- Add/extend tests and manual smoke scenarios that assert managed workspace layout mode transitions.
*** DONE Run full self-validation before user review
- Run full automated regression coverage (=make test=) and include new Task 11 tests.
- Run end-to-end manual validation in the agent's own nested Hyprland instance for both:
  - regression scenarios from completed tasks
  - new Task 11 layout-mode behavior (=tiledLayout= transition, strict Emacs ownership, immediate hide-before-layout, restore on unmanage/disconnect)
- Fix discovered bugs and update tests/docs before requesting the user review checkpoint.
*** DONE Review task 11 with user
- Stop when managed workspace reports =tiledLayout: hyprmacs= and restores cleanly on unmanage/disconnect.

*** DONE Session note: Task 11 design constraints locked with user (2026-04-18)
- Layout ownership model is strict: managed workspaces use explicit =hyprmacs= layout mode and do not allow native fallback tiling for managed clients.
- New eligible clients must be hidden immediately until Emacs publishes visibility/geometry via =set-layout=.
- 8A policy constraints are tied to managed-layout ownership, but implementation must remain future-compatible with multi-workspace support:
  - use a global policy lease manager semantics (acquire on first managed workspace, restore on last release)
  - avoid per-workspace restore assumptions for global Hyprland options.

*** DONE Session note: Task 11 implementation details (2026-04-18)
- Workspace manager now snapshots and restores per-workspace tiled layout via =j/workspaces= (fallback =general:layout=) and dispatches:
  - =keyword workspace <id>,layout:hyprmacs= on manage
  - =keyword workspace <id>,layout:<previous>= on unmanage/disconnect.
- Managed policy enforcement moved to lease semantics for future multi-workspace compatibility:
  - acquire policy snapshot/enforcement on first lease
  - restore snapshot on final lease release.
- Plugin now hides managed clients immediately on:
  - =manage-workspace= (existing managed clients)
  - transition to managed/tiled state (newly eligible clients while managed).
- Plugin registers a Hyprland tiled algorithm named =hyprmacs= during plugin init and removes it on plugin exit.

*** DONE Session note: Task 11 verification evidence (2026-04-18)
- Automated verification:
  - =just test= (Emacs suites) passed.
  - =nix develop -c bash -lc 'cd /home/hansffu/code/hyprmacs && make test'= passed (C++ + Emacs suites).
- Manual nested validation in agent-controlled session:
  - During active managed controller: =hyprctl -j activeworkspace | jq .tiledLayout= returned =hyprmacs=.
  - After controller disconnect: same command returned restored layout (=dwindle= in test session).
  - Existing eligible client moved to =special:hyprmacs-hidden= immediately on manage before layout publication.
  - Newly opened eligible client while managed was also moved immediately to =special:hyprmacs-hidden=.
  - On disconnect cleanup, hidden clients returned to workspace =1=.

*** DONE Session note: Task 11 review-feedback fixes (2026-04-18)
- User reported runtime regressions during Task 11 review:
  - Emacs-control handoff could target the wrong Emacs frame when multiple Emacs windows existed in the workspace.
  - Non-managed tiled windows (for example Brave/extra Emacs windows) could overlap and produce confusing render/input behavior under =layout:hyprmacs=.
- Implemented fixes:
  - Added tracking for the workspace's managing Emacs frame in =WorkspaceManager= and prioritized it for Emacs-control focus handoff.
  - Managing Emacs frame selection now remains stable across incidental focus changes and falls back safely if the managing frame closes.
  - Changed the registered =hyprmacs= tiled algorithm to non-intrusive recalc behavior so compositor layout mode no longer force-overwrites tiled target geometry.
- Added regression coverage:
  - workspace-manager test for stable managing-Emacs-frame selection and fallback on close.
  - IPC route test asserting =set-input-mode(emacs-control)= focuses the tracked managing Emacs frame.

*** DONE Session note: Task 11 additional review-feedback fix (2026-04-18)
- User reported Emacs state not updating when compositor focus/close changes happened outside Emacs commands (for example click-to-focus and in-app close button).
- Added workspace-state change notifier path from =WorkspaceManager= to =IpcServer= so managed-workspace state mutations now push =state-dump= to Emacs controller without manual =request-state=.
- Added regression coverage for focus-change and closewindow event-driven state notifications.
- Added Emacs-side selected-client activation on inbound =state-dump= / =layout-applied= so when compositor focus changes managed client selection, the corresponding managed buffer window becomes the active Emacs window.

*** DONE Session note: Task 11 follow-mouse policy rollback by user request (2026-04-18)
- User requested disabling plugin-side =follow_mouse= handling because runtime behavior under =move-only= was inconsistent and they prefer explicit user-owned config.
- Updated managed workspace policy lease scope:
  - keep plugin-managed constraints on =animations:enabled= and =misc:focus_on_activate=
  - stop snapshot/apply/restore of =input:follow_mouse= in plugin lifecycle.
- Updated workspace-manager regression coverage and manual smoke docs to match this narrowed policy contract.

*** DONE Session note: nested runtime follow-mouse default tuned after live repro (2026-04-18)
- Live reproduction with logs showed compositor focus flips on mouse movement while a managed client was in positioning mode.
- In nested debug config, set =input.follow_mouse= to =2= as the default runtime behavior.
- Reason: with this setting in the reproduced session, hover focus stopped while click-to-focus still worked as expected.
- Additional live finding: =input.float_switch_override_focus= caused unwanted focus steals in a user instance even with follow-mouse tuned.
- Nested debug config now also sets:
  - =input.mouse_refocus = false=
  - =input.float_switch_override_focus = 0=
- Plugin still does not manage this option; user config remains the source of truth.

Validation:
- Manage workspace and verify =hyprctl -j activeworkspace= reports =tiledLayout= as =hyprmacs=.
- Unmanage/disconnect and verify prior layout mode is restored.

** DONE Task 11B: Automate full nested E2E regression flow
Purpose:
- Replace manual-only nested validation with a repeatable script that exercises implemented behavior across Tasks 1-11.

Files:
- Create =scripts/hyprmacs-e2e-nested.sh=
- Modify =emacs/hyprmacs.el=
- Modify =justfile=
- Modify =docs/testing/manual-smoke-tests.org=
- Modify =docs/superpowers/plans/2026-04-15-hyprmacs-iterative-implementation.org=

Steps:
*** DONE Define E2E harness contract
- Run static/unit coverage first (=make test=) before launching nested runtime checks.
- Launch a fresh nested Hyprland instance and auto-detect the active instance signature.
- Run scripted Emacs-driven workflow assertions for manage/state/layout/mode/unmanage behavior.
*** DONE Implement nested orchestration script
- Start nested compositor, spawn test windows, launch controllable Emacs daemon/client, and run E2E Lisp entrypoint.
- Collect deterministic logs/artifacts for failures.
- Ensure cleanup (nested compositor + daemon) on success and failure.
*** DONE Add runtime assertions that map to Tasks 1-11
- Validate plugin event tap and IPC message flow.
- Validate managed workspace layout mode transitions and policy values.
- Validate managed client discovery, selection/mode switching, and cleanup on unmanage.
*** DONE Add developer entrypoint
- Add a =just= recipe that runs the full nested E2E harness.
*** DONE Review task 11B with user
- Stop after the automated harness succeeds locally and provide generated artifact paths for user validation.

*** DONE Session note: Task 11B automated harness implementation (2026-04-18)
- Added =scripts/hyprmacs-e2e-nested.sh= as a full automated nested regression harness.
- Added =M-x hyprmacs-run-full-e2e-test= with fail-fast assertions for runtime manage/layout/policy/mode/unmanage flow.
- Added =just e2e= developer entrypoint (runs harness under =nix develop=).
- Added manual smoke doc section describing the automated harness and artifact locations.

*** DONE Session note: Task 11B local verification evidence (2026-04-18)
- Ran =just e2e= successfully in nested mode.
- Harness completed with:
  - all plugin + Emacs unit suites passing
  - nested runtime assertions passing
  - plugin runtime signal checks (event tap + IPC command flow) passing
- Example artifact bundle:
  - =artifacts/e2e/20260418-211500/summary.log=
  - =artifacts/e2e/20260418-211500/session-e2e.log=

*** DONE Session note: Task 11B use-case coverage expansion (2026-04-18)
- Expanded runtime E2E assertions based on user-requested lifecycle scenarios:
  - managed-buffer window visibility roundtrip (show -> hide -> show) with compositor workspace verification
  - command-path buffer close (=kill-current-buffer=) followed by automatic recreation from inbound state
  - explicit retention of same-title distinct-client coverage, float/tile toggling, close-window command cleanup, and multi-frame focus handoff.
- Updated =docs/testing/manual-smoke-tests.org= automated harness section to list the expanded scenario set.

*** DONE Session note: Task 11B regression fix for client-control selection source (2026-04-18)
- User-reported behavior: =M-x hyprmacs-set-input-mode= could focus the wrong managed client (observed as first client order fallback) instead of the client associated with the current managed buffer.
- Root cause:
  - Emacs command path only sent =set-input-mode(client-control)=.
  - Plugin behavior when no selected client exists falls back to first managed client for focus.
- Added failing test first:
  - =tests/emacs/protocol-tests.el::hyprmacs-set-input-mode-selects-client-for-current-managed-buffer=
  - verifies command emits =set-selected-client= for the current managed buffer before =set-input-mode=.
- Implemented fix:
  - =emacs/hyprmacs.el=: on client-control mode switch, derive current buffer client ID when it is managed and send =set-selected-client= first (when changed), then send mode switch.
- Verification:
  - targeted ERT protocol suite passes
  - full =just e2e= passes (artifact bundle: =artifacts/e2e/20260418-232545/=).

** DONE Task 12: Introduce dedicated managed-client major mode
Purpose:
- Add a focused Emacs major mode for managed client buffers before broader packaging/documentation work.

Files:
- Modify =emacs/hyprmacs-buffers.el=
- Modify =emacs/hyprmacs-session.el=
- Modify =tests/emacs/buffer-tests.el=
- Modify =docs/testing/manual-smoke-tests.org=

Steps:
*** DONE Add =hyprmacs-window-mode=
- Define a dedicated major mode for managed client buffers.
- Keep it minimal for prototype scope (identity + predictable mode map scaffold).
*** DONE Attach mode during buffer association lifecycle
- Ensure managed buffers created/recreated by client association always enter =hyprmacs-window-mode=.
*** DONE Add regression tests
- Verify managed buffers use =hyprmacs-window-mode= on create and recreate paths.
*** DONE Validate in nested runtime
- Run =just e2e= and confirm managed buffer behavior remains stable with mode enabled.
*** DONE Review task 12 with user
- Use this checkpoint to validate mode behavior before packaging/demo polish.

Validation:
- In nested session, managed buffers report =major-mode= as =hyprmacs-window-mode=.
- Existing buffer lifecycle and E2E tests remain green.

*** DONE Session note: Task 12 implementation for managed-window major mode (2026-04-19)
- Added =hyprmacs-window-mode= and keymap scaffold in =emacs/hyprmacs-buffers.el=.
- Bound =i= in mode map to =hyprmacs-set-input-mode= (client-control entry path from Emacs-managed buffers).
- Added configurable rename handler:
  - =hyprmacs-window-rename-function=
  - default implementation =hyprmacs-window-default-rename-function=
- Added hook points:
  - =hyprmacs-window-title-change-functions=
  - =hyprmacs-window-selected-functions=
  - =hyprmacs-window-input-mode-change-functions=
- Added mode-line input-mode indicator (=Hyprmacs[E/C/?]=) with refresh on session selection/mode transitions.
- Extended buffer-local metadata:
  - =hyprmacs-client-title=
  - =hyprmacs-workspace-id=
- Updated session path to call =hyprmacs-buffer-notify-session-state-changed= after inbound frame handling so mode-line/hook behavior stays synchronized.

*** DONE Session note: Task 12 verification evidence (2026-04-19)
- Failing tests were added first in =tests/emacs/buffer-tests.el= to assert:
  - mode activation and keymap binding
  - rename handler override behavior
  - title-change hook emission
  - mode-line input indicator updates
  - session state-change hook emission for selected/input transitions
- Verification commands:
  - =emacs -Q --batch -L emacs -L tests/emacs -l tests/emacs/buffer-tests.el -f ert-run-tests-batch-and-exit=
  - =emacs -Q --batch -L emacs -L tests/emacs -l tests/emacs/session-tests.el -f ert-run-tests-batch-and-exit=
  - =emacs -Q --batch -L emacs -L tests/emacs -l tests/emacs/protocol-tests.el -f ert-run-tests-batch-and-exit=
  - =just e2e=
- Latest nested harness artifact bundle:
  - =artifacts/e2e/20260419-000421/summary.log=
  - =artifacts/e2e/20260419-000421/session-e2e.log=

*** DONE Session note: Task 12 review-feedback adjustments (2026-04-19)
- Updated default mode-map binding for entering client-control from managed buffers:
  - from =i=
  - to =C-c C-c=
- Updated default managed buffer naming policy:
  - from client-id-based default (=*hyprmacs:<client-id>:<title>*=)
  - to class/title default (=*hyprmacs:<class>: <title>*=)
- Updated tests and nested harness validation with passing results:
  - latest artifact bundle: =artifacts/e2e/20260419-001943/=

** DONE Task 13: Add custom Hyprland dispatchers for plugin control
Purpose:
- Provide compositor-native commands (for example =Alt+E=) that can control hyprmacs even when Emacs does not own keyboard input.
- Establish the dispatcher path that future hyprmacs actions can reuse.

Files:
- Modify =plugin/src/plugin_main.cpp=
- Modify =plugin/src/ipc_server.cpp= (if routing helpers are needed)
- Modify =plugin/tests/ipc_server_tests.cpp=
- Modify =flake.nix= (nested config keybind wiring)
- Modify =docs/testing/manual-smoke-tests.org=
- Modify =docs/superpowers/plans/2026-04-15-hyprmacs-iterative-implementation.org=

Steps:
*** DONE Add dispatcher registration in plugin
- Register one or more hyprmacs dispatchers via Hyprland plugin API.
- Implement at minimum =hyprmacs:set-emacs-control-mode=.
*** DONE Bridge dispatcher to existing workspace control path
- Resolve target workspace from dispatcher arg (fallback to managed workspace in current single-workspace prototype).
- Route dispatcher action into existing input-mode handling (=set-input-mode emacs-control= equivalent behavior).
*** DONE Wire nested config keybind
- Bind =ALT+E= in nested Hyprland config to call the new dispatcher.
*** DONE Add regression tests
- Validate dispatcher command updates mode and returns focus to managing Emacs frame without hanging Hyprland IPC.
- Keep current IPC command-path tests green.
*** DONE Review task 13 with user
- Validate real interactive handoff: client-control -> =ALT+E= -> emacs-control focus return.

Validation:
- In nested runtime, =ALT+E= returns control to managing Emacs frame while workspace is managed.
- Plugin tests cover dispatcher behavior and existing mode-switch contracts.

*** DONE Session note: Task 13 dispatcher implementation (2026-04-19)
- Added new dispatcher bridge module:
  - =plugin/include/hyprmacs/dispatchers.hpp=
  - =plugin/src/dispatchers.cpp=
- Implemented =hyprmacs:set-emacs-control-mode= behavior:
  - resolve workspace from dispatcher arg or active workspace query (fallback managed workspace)
  - set input mode to =emacs-control=
  - focus managing Emacs frame when available
- Registered dispatcher via Hyprland plugin API (=addDispatcherV2=) in =plugin_main.cpp= and removed it on plugin exit.
- Added =IpcServer::publish_state_dump_for_workspace= so dispatcher path pushes state updates to connected controller.
- Updated nested Hyprland bind in =flake.nix=:
  - =ALT+E= now invokes dispatcher =hyprmacs:set-emacs-control-mode=.

*** DONE Session note: Task 13 verification evidence (2026-04-19)
- Added plugin regression target:
  - =plugin/tests/dispatchers_tests.cpp=
  - =hyprmacs_dispatcher_tests= added to plugin test CMake.
- Verification commands:
  - =nix develop -c sh -lc 'cmake -S plugin -B build/plugin && cmake --build build/plugin && ctest --test-dir build/plugin --output-on-failure'=
  - =just e2e=
- Latest nested harness artifact bundle:
  - =artifacts/e2e/20260419-003841/summary.log=
  - =artifacts/e2e/20260419-003841/session-e2e.log=

*** DONE Session note: Task 13 review bugfix for =ALT+E= input handoff hang (2026-04-19)
- User review reported reproducer:
  - =client-control= via =C-c C-c= in managed buffer works
  - =ALT+E= makes all input appear dead.
- Root cause identified:
  - Dispatcher callback executed synchronous socket-based =hyprctl dispatch/query= from inside Hyprland dispatcher thread, causing IPC timeout/hang (for example: =Hyprland IPC didn't respond in time=).
- Fix implemented:
  - Refactored dispatcher contract to only set mode and return optional focus target.
  - Removed synchronous socket query for active workspace from dispatcher callback path.
  - Dispatcher callback now:
    - resolves workspace via dispatcher arg or managed-workspace fallback
    - sets input mode to =emacs-control=
    - schedules focus to managing Emacs client asynchronously after callback return.
- Regression coverage added:
  - Nested e2e now explicitly asserts =hyprctl dispatch hyprmacs:set-emacs-control-mode= succeeds.
  - Direct scripted repro confirms:
    - active window =foot= in client-control
    - dispatcher returns =ok=
    - active window returns to managing =emacs= client.
- Verification evidence:
  - =just e2e= PASS with new dispatcher assertion:
    - =artifacts/e2e/20260419-005640/summary.log=
    - =artifacts/e2e/20260419-005640/session-e2e.log=

*** DONE Session note: Post-Task 13 behavior change - killing managed buffer closes client (2026-04-19)
- User requested that killing a managed Emacs buffer should close the corresponding Hyprland client window.
- Implemented in Emacs managed-buffer mode:
  - kill interception dispatches =hyprctl dispatch closewindow address:<client-id>= via configurable close handler.
  - buffer kill is canceled while close request is in-flight; buffer is removed when client-close state update arrives.
  - internal cleanup paths (=hyprmacs-buffer-remove-client=, reset) suppress close dispatch interception.
- Updated automated and manual validation:
  - Emacs unit tests assert:
    - manual kill requests close and is intercepted (buffer not immediately killed)
    - internal remove does not trigger close handler
  - Nested e2e expects managed client removal after kill request and subsequent close event/state update.
  - Manual smoke checklist documents kill-buffer closes client behavior.

** DONE Task 14: Package the prototype, document usage, and freeze the first demo flow
Purpose:
- Make the prototype repeatable and easy to evaluate.

Files:
- Modify =flake.nix=
- Create =README.org=
- Create =docs/testing/demo-script.org=
- Modify =docs/testing/manual-smoke-tests.org=

Steps:
*** DONE Add a reliable demo entrypoint
- Provide one command that builds the plugin and launches nested Hyprland with the plugin loaded.
*** DONE Document the user workflow
- Explain how to start the demo, load the Emacs package, connect, manage a workspace, and switch modes.
*** DONE Freeze a smoke-test checklist
- Record the exact scenarios that must pass before any future refactor is considered safe.
*** DONE Review task 14 with user
- Use this checkpoint to decide whether to continue toward polish work or pause after the first usable prototype.

Validation:
- Follow =README.org= from a clean shell and reproduce the demo without manual patching.

*** DONE Session note: Task 14 implementation for packaging/docs/demo flow (2026-04-19)
- Added a stable demo entrypoint alias in flake outputs:
  - =nix run .#demo= (mapped to nested Hyprland launcher with plugin preloaded).
- Added prototype operator documentation:
  - =README.org= with quick-start, controls, semantics, and verification pointers.
  - =docs/testing/demo-script.org= with step-by-step demo script.
- Froze prototype gate checklist in:
  - =docs/testing/manual-smoke-tests.org= under =Prototype Gate Checklist=.

*** DONE Session note: Task 14 review-feedback docs update for real NixOS Hyprland (2026-04-19)
- Added first-class documentation for non-nested install/runtime on NixOS:
  - =docs/setup/nixos-real-hyprland.org=
- Added README pointer so real-session instructions are discoverable from top-level docs.
- Scope of this update is documentation only (no compositor/runtime behavior change).

*** DONE Session note: Task 14 review bugfix for new tiled client adoption race (2026-04-19)
- User review reported a real-session regression with =pcmanfm=:
  - new client first opened beside Emacs instead of inside the managed buffer
  - native tiling resized the Emacs frame before hyprmacs recovered
- Root cause identified in the plugin transition path:
  - newly managed-client adoption/hide was coupled to controller-connected IPC notification state
  - this made immediate hide depend on IPC timing instead of on managed-state transition itself
- Fix implemented:
  - workspace-manager now emits managed-client tiled/floating transition callbacks whenever membership changes in the managed workspace, even if controller connection state is not yet marked connected
  - IPC server now performs hide/show on those transitions independently of whether a controller socket is currently available for message delivery
- Added plugin regression coverage to assert a newly opened managed tiled client still emits the transition callback without controller connection state.

*** DONE Session note: Task 14 review bugfix for local e2e build environment and plugin link closure (2026-04-19)
- User review reported =just e2e= failing before runtime validation.
- First root cause:
  - the dev shell exposed =hyprland.pc= but not the full pkg-config dependency closure required by its =Requires= chain
  - updated the flake dev shell to derive =PKG_CONFIG_PATH= from =hyprPkg.buildInputs= plus =hyprPkg.dev= so local plugin builds inside =nix develop= track Hyprland dependency changes automatically
- Second root cause uncovered after the shell fix:
  - =plugin/CMakeLists.txt= consumed Hyprland include flags from pkg-config but did not link the plugin target against =PkgConfig::HYPRLAND=
  - the resulting =libhyprmacs.so= built successfully but failed the symbol smoke test at =dlopen= time with unresolved Hyprland layout symbols
- Fix implemented:
  - link =hyprmacs= against =PkgConfig::HYPRLAND=
  - keep the dev-shell pkg-config closure fix so =just e2e= and local =make build= use the same complete dependency view.
- Follow-up correction:
  - standalone =dlopen= is not a valid smoke check for a real Hyprland plugin because host-provided Hyprland symbols are resolved by the compositor process, not by the test binary
  - updated the smoke test to inspect exported plugin entry symbols from the built shared object instead of requiring out-of-process Hyprland symbol resolution.
  - aligned the symbol check with the real Hyprland export names from =PluginAPI.hpp=:
    - =pluginAPIVersion=
    - =pluginInit=
    - =pluginExit=

*** DONE Session note: Task 14 review-followup for floating window validation (2026-04-21)
- User requested stronger floating-window validation before approving the Task 14 WAIT checkpoint.
- Nested Hyprland config updates in =flake.nix=:
  - Added =windowrulev2 = float,class:^(mpv)$= so mpv windows open as native floating surfaces.
  - Added =ALT+F= keybind for native =togglefloating= dispatch in nested runs.
  - Added =pkgs.mpv= to dev tools so nested E2E can spawn mpv deterministically.
- Extended nested full E2E flow in =hyprmacs-run-full-e2e-test=:
  - Spawns an =mpv= test window and asserts it remains floating and stays on the managed workspace.
  - Verifies the floating mpv client is excluded from managed and eligible sets and has no managed Emacs buffer association.
  - Re-validates those exclusions after Emacs focus and managed-client layout switches, ensuring managed layout sync does not absorb floating windows.

*** DONE Session note: Task 14 review-followup manual event-tap capture for event selection (2026-04-21)
- User requested a full Hyprland event capture pass from the same nested runtime path used by E2E (=nix run .#default=).
- Added debug path in =WorkspaceManager=: when =HYPRMACS_LOG_ALL_EVENTS= is truthy, plugin logs every =.socket2.sock= frame as =event-all= before tracked-event filtering.
- Nested launcher now exports =HYPRMACS_LOG_ALL_EVENTS=1= so manual/e2e nested sessions can capture complete event streams.
- Ran a manual nested capture flow with window open/focus/title changes, workspace manage/unmanage, hide/show moves, close, and togglefloating actions; artifact bundle:
  - =artifacts/manual-events/20260421-222735/summary.log=
  - =artifacts/manual-events/20260421-222735/hyprland.log=
  - =artifacts/manual-events/20260421-222735/event-name-counts.txt=
- Observed event names in this runtime:
  - =openwindow=, =closewindow=
  - =activewindow=, =activewindowv2=
  - =windowtitle=, =windowtitlev2=
  - =movewindow=, =movewindowv2=
  - workspace/monitor metadata events (=workspace*=, =createworkspace*=, =destroyworkspace*=, =moveworkspace*=, =focusedmon*=, =monitoradded*=, =monitorremoved*=, =activelayout=)
- Not observed in this run, including after explicit =togglefloating= actions:
  - =changefloatingmode=
  - =changefloatingmodev2=
- Practical implication for current runtime:
  - floating-state transitions cannot rely solely on dedicated floating events.
  - keep using refresh-from-=j/clients= on reliable lifecycle events (open/focus/title) to infer floating state changes.

*** DONE Session note: Task 14 review-followup architecture spec for non-floating managed geometry (2026-04-21)
- User requested a technical specification for replacing managed-client floating geometry with a tiled algorithm path.
- Added markdown architecture spec:
  - =docs/architecture/non-floating-managed-layout-spec.md=
- Spec scope includes:
  - Hyprland custom tiled algorithm API surface relevant to this repo
  - plugin data/control flow for snapshot-driven non-floating geometry
  - migration plan (feature-flagged dual path -> default tiled path -> remove floating geometry path)
  - validation and acceptance criteria.

*** DONE Session note: Task 14 review-followup requirement update for managing Emacs window fullscreen (2026-04-21)
- User clarified a hard requirement for the proposed tiled-geometry architecture:
  - managing Emacs window should always occupy full workspace area while managed.
  - plugin (not Emacs) should own this placement if plugin owns all placement logic.
- Updated =docs/architecture/non-floating-managed-layout-spec.md= to include:
  - fullscreen Emacs invariant in goals/data model/control flow/behavior rules
  - recalculate step that pins managing Emacs target to =space()->workArea()=
  - validation and acceptance criteria for persistent Emacs fullscreen behavior.

*** DONE Session note: Task 14 review-followup on managed-window reposition policy (2026-04-21)
- User clarified two additional requirements for the architecture spec:
  - app-specific examples (such as =mpv=) should not be part of the normative design text.
  - managed clients and managing Emacs should not be user-repositionable while managed; if hard prevention is not guaranteed, plugin must detect geometry drift and snap back without infinite loops.
- Updated =docs/architecture/non-floating-managed-layout-spec.md= accordingly:
  - removed app-specific wording in automated validation criteria.
  - added explicit interactive repositioning policy with prevent-first + corrective snapback model.
  - added loop-guard requirements (expected-box compare, version token, debounced scheduling, bounded retries/backoff, managed-target-only scope).

*** DONE Session note: Task 14 WAIT checkpoint approval and final verification (2026-04-21)
- User requested to wrap up the prototype and approved continuing past the Task 14 review checkpoint.
- Re-ran verification before closeout:
  - =just test= passed.
  - =just e2e= passed with artifact bundle =artifacts/e2e/20260421-234020/=.
- Task 14 review checkpoint is now closed and Task 14 is marked complete.

*** DONE Session note: authoritative non-floating managed-layout contract update (2026-04-22)
- User requested turning =docs/architecture/non-floating-managed-layout-spec.md= into the authoritative implementation contract with explicit interface ownership and behavior semantics.
- Locked ownership model:
  - Emacs is the only planner for managed non-floating visibility/placement via =set-layout= snapshots.
  - Plugin is the executor/state authority and applies accepted snapshots via tiled algorithm recalc.
- Locked hard cutover requirement:
  - no dual-path migration/fallback for managed geometry.
  - managed geometry must not rely on =togglefloating= /=movewindowpixel= /=resizewindowpixel=.
- Locked window-class layering rule:
  - floating windows > managed non-floating windows > managing Emacs window.
- Locked managing Emacs invariant:
  - managing Emacs client is non-floating and pinned to workspace usable area (=space()->workArea()=) with bounded snapback on drift.
- Locked transition behavior:
  - managed client becoming floating leaves managed set immediately in plugin state.
  - Emacs removes managed buffer association when next authoritative =state-dump= no longer lists that client in =managed_clients=.
- Locked plugin-notification debounce policy:
  - debounced/coalesced state notifications are configurable via plugin setting =plugin:hyprmacs:state_notify_debounce_ms= (not env var).
  - default =30= ms, =0= means immediate publish, with bounded validation/fallback.

** DONE Task 15: Implement authoritative non-floating managed layout contract
Purpose:
- Replace managed floating-dispatch geometry path with tiled-algorithm execution while preserving Emacs-owned layout planning and native floating window behavior.

Files:
- Modify =plugin/include/hyprmacs/workspace_manager.hpp=
- Modify =plugin/src/workspace_manager.cpp=
- Modify =plugin/include/hyprmacs/ipc_server.hpp=
- Modify =plugin/src/ipc_server.cpp=
- Modify =plugin/include/hyprmacs/layout_applier.hpp=
- Modify =plugin/src/layout_applier.cpp=
- Modify =plugin/src/plugin_main.cpp=
- Modify =plugin/tests/workspace_manager_tests.cpp=
- Modify =plugin/tests/ipc_server_tests.cpp=
- Modify =plugin/tests/layout_applier_tests.cpp=
- Modify =tests/emacs/session-tests.el=
- Modify =tests/emacs/protocol-tests.el=
- Modify =docs/protocol/ipc-v1.org=
- Modify =docs/testing/manual-smoke-tests.org=

Steps:
*** DONE Add workspace-scoped canonical layout snapshot state in plugin
- Add managed-workspace snapshot model in =WorkspaceManager= with layout versioning and managing-Emacs target identity.
- Add apply/get/clear helpers so algorithm recalc can consume authoritative state directly from manager.

*** DONE Route =set-layout= through snapshot validation and state commit only
- Keep =IpcServer= as parser/validator/router for =set-layout=.
- Validate required arrays, per-visible rectangle presence, and overlap rejection before state commit.
- Emit =layout-applied= and =state-dump= using committed manager state.

*** DONE Move managed geometry authority to tiled algorithm recalc
- Update =CHyprmacsAlgorithm::recalculate()= to:
  - pin managing Emacs client to =space()->workArea()=
  - place visible managed non-Emacs targets from latest snapshot rectangles
  - ignore unmanaged and native floating targets
- Ensure managing Emacs placement invariant survives workarea and lifecycle changes.

*** DONE Remove floating-dispatch geometry path from managed layout execution
- Remove managed geometry reliance on =togglefloating=, =movewindowpixel=, and =resizewindowpixel= in =LayoutApplier=.
- Keep =LayoutApplier= focused on hide/show/native-restore behavior only.

*** DONE Enforce floating/non-floating transition semantics and layer ordering contract
- Preserve and harden transition flow:
  - managed -> floating: plugin drops managed membership immediately and notifies Emacs
  - floating -> non-floating: plugin re-evaluates eligibility and publishes updated state
- Preserve normative stacking contract in behavior/tests:
  - floating windows > managed non-floating windows > managing Emacs window.
- Keep Emacs buffer removal on managed->floating driven by next authoritative =state-dump=.

*** DONE Add debounced/coalesced plugin state-notify publishing with plugin setting
- Add plugin option lookup for =plugin:hyprmacs:state_notify_debounce_ms=.
- Implement non-resetting coalescing timer behavior in =IpcServer= state-publish path.
- Defaults/validation:
  - default 30ms
  - 0ms means immediate publish
  - clamp 0..1000 with warning on invalid/missing value

*** DONE Fix review feedback regression: =just run= must start nested Hyprland session
- Investigate root cause for =just run= no longer launching nested Hyprland runtime required for manual validation.
- Restore nested-session startup behavior without regressing Task 15 runtime wiring.
- Add/update coverage or smoke-validation notes for the restored launch path.

*** DONE Review task 15 with user
- Stop when tiled recalc owns managed geometry, floating windows remain fully native/top-layer, and state-notify debounce behavior is observable and configurable via plugin setting.

Validation:
- =just test=
- =just e2e=
- Manual checks in nested runtime:
  - managing Emacs window remains pinned to work area during managed session.
  - managed non-floating clients follow Emacs rectangles without entering floating mode.
  - floating windows remain unmanaged and visually above managed + Emacs surfaces.
  - toggling a managed client to floating removes it from =managed_clients= in next debounced =state-dump= and Emacs removes managed buffer association.
  - toggling a floating eligible client back to non-floating allows re-adoption through next planning cycle.

*** DONE Session note: Task 15 step 1 implementation and review closure (2026-04-22)
- Implemented WorkspaceManager-owned committed managed-layout snapshot state with manager-authoritative metadata and monotonic versioning.
- Added =apply/get/clear= snapshot helpers and enforced managed-workspace-only apply semantics for the current single-workspace prototype.
- Hardened snapshot coherence across mutation paths:
  - setter-driven updates (=set_selected_client=, =set_input_mode=)
  - lifecycle/event updates (focus, close, move, floating transitions)
  - =seed-client= reconciliation
  - hidden-by-default insertion for newly managed clients until Emacs replans.
- Added focused C++ regression coverage in =plugin/tests/workspace_manager_tests.cpp= for versioning, clear/switch/disconnect cleanup, manager-authoritative field sync, lifecycle pruning, and seed-client coherence.
- Review gates completed before moving on:
  - spec-compliance reviewer: pass
  - code-quality reviewer: pass
- Local verification run in this session:
  - =nix develop -c cmake --build build/plugin --target hyprmacs_workspace_manager_tests -j2=
  - =nix develop -c ctest --test-dir build/plugin --output-on-failure -R hyprmacs_workspace_manager_tests=
  - result: pass.

*** DONE Session note: Task 15 step 2 implementation and review closure (2026-04-22)
- Updated =set-layout= router path in =plugin/src/ipc_server.cpp= to validate payload contract before state mutation, then commit through =WorkspaceManager::apply_managed_layout_snapshot(...)=, and only apply optional selected/input updates after successful commit.
- Removed managed-geometry execution from the =set-layout= path (no =LayoutApplier::apply_snapshot= calls in this route).
- Added/expanded IPC regression coverage in =plugin/tests/ipc_server_tests.cpp= for:
  - required-array validation
  - membership validation against managed/eligible clients
  - canonical set constraints (duplicates, visible/hidden overlap, stacking-order permutation)
  - overlap rejection and missing-rectangle rejection
  - failed-layout no-partial-state guarantee
  - success-path snapshot commit + response flow.
- Review gates completed before moving on:
  - spec-compliance reviewer: pass
  - code-quality reviewer: pass

*** DONE Session note: Task 15 step 3 implementation and review closure (2026-04-22)
- Moved managed non-floating geometry ownership into =CHyprmacsAlgorithm::recalculate()= and kept managing Emacs pinned to workspace work area.
- Kept floating and unmanaged targets outside managed-geometry placement path.
- Fixed hidden->visible restore semantics for managed clients by allowing visibility action recovery for currently hidden targets and applying geometry after show.
- Preserved compositor thread safety for post-commit recalc by marshalling recalc requests through dispatcher command path; no direct IPC-thread recalc call.
- Made recalc request failures non-silent in =set-layout= route:
  - updated =RecalcRequester= to return =bool=
  - logs warning when recalc scheduling fails after successful snapshot commit.
- Added/expanded regression coverage:
  - =plugin/tests/plugin_main_tests.cpp= visibility behavior for hidden-target restoration
  - =plugin/tests/ipc_server_tests.cpp= warning-path coverage for recalc request failure.
- Review gates completed before moving on:
  - spec-compliance reviewer: pass
  - code-quality reviewer: pass
- Local verification run in this session:
  - =nix develop -c sh -lc 'cmake -S plugin -B build/plugin >/tmp/hyprmacs-cmake.log && cmake --build build/plugin --target hyprmacs_plugin_main_tests hyprmacs_ipc_server_tests hyprmacs_workspace_manager_tests hyprmacs_layout_applier_tests -j2 && ctest --test-dir build/plugin --output-on-failure -R "hyprmacs_(plugin_main_tests|ipc_server_tests|workspace_manager_tests|layout_applier_tests)"'=
  - result: pass.

*** DONE Session note: Task 15 step 4 implementation and review closure (2026-04-22)
- Removed remaining managed floating-dispatch geometry path from =LayoutApplier=:
  - deleted =togglefloating= positioning-mode state handling
  - deleted =movewindowpixel= /=resizewindowpixel= execution path
  - kept hide/show and workspace-restore behavior only.
- Kept =apply_snapshot(...)= as a hide/show coordinator with overlap validation and visibility transitions, without geometry command execution.
- Updated =layout_applier= unit coverage to match new responsibilities:
  - hide/show roundtrip + id normalization + idempotency
  - snapshot hide/show behavior without geometry commands
  - native restore showing hidden clients only.
- Review gates completed before moving on:
  - spec-compliance reviewer: pass
  - code-quality reviewer: pass
- Local verification run in this session:
  - =nix develop -c sh -lc 'cmake -S plugin -B build/plugin >/tmp/hyprmacs-cmake.log && cmake --build build/plugin --target hyprmacs_layout_applier_tests hyprmacs_ipc_server_tests hyprmacs_plugin_main_tests hyprmacs_workspace_manager_tests -j2 && ctest --test-dir build/plugin --output-on-failure -R "hyprmacs_(layout_applier_tests|ipc_server_tests|plugin_main_tests|workspace_manager_tests)"'=
  - result: pass.

*** DONE Session note: Task 15 step 5 implementation and review closure (2026-04-22)
- Hardened managed/floating transition semantics with explicit transition-path coverage in =plugin/tests/ipc_server_tests.cpp=:
  - managed -> floating removes client from managed membership immediately and restores native visibility
  - floating -> tiled re-adopts client into managed membership and restores managed hide path.
- Hardened recalc layering behavior by switching to explicit class-ordered processing in =CHyprmacsAlgorithm::recalculate()=:
  - managing Emacs processed first (baseline)
  - managed visible non-Emacs targets processed in snapshot =stacking_order=
  - hidden managed targets processed in hidden pass.
- Preserved floating-native behavior by keeping floating targets outside managed visibility/geometry application.
- Review gates completed before moving on:
  - spec-compliance reviewer: pass
  - code-quality reviewer: pass
- Local verification run in this session:
  - =nix develop -c sh -lc 'cmake -S plugin -B build/plugin >/tmp/hyprmacs-cmake.log && cmake --build build/plugin --target hyprmacs_plugin_main_tests hyprmacs_ipc_server_tests hyprmacs_workspace_manager_tests hyprmacs_layout_applier_tests -j2 && ctest --test-dir build/plugin --output-on-failure -R "hyprmacs_(plugin_main_tests|ipc_server_tests|workspace_manager_tests|layout_applier_tests)"'=
  - result: pass.

*** DONE Session note: Task 15 step 6 implementation and review closure (2026-04-22)
- Implemented debounced/coalesced plugin state-notify publishing in =IpcServer= with plugin setting lookup:
  - setting =plugin:hyprmacs:state_notify_debounce_ms=
  - default =30= ms when missing
  - =0= means immediate publish
  - values clamped to =0..1000= with warning on out-of-range input.
- Added debounce normalization helper coverage in =plugin/tests/ipc_server_tests.cpp= for default, immediate, low clamp, and high clamp paths.
- Implemented non-resetting workspace coalescing semantics:
  - first state change opens debounce window per workspace
  - subsequent state changes within the window coalesce without extending deadline.
- Routed transition-triggered state-dump publishing through same state-notify path.
- Fixed session-isolation race in debounce publishing with controller-generation tagging:
  - queued publishes carry source controller generation
  - publish path drops stale generations
  - generation increments on connect/disconnect/stop and pending queue clears on connect/disconnect boundaries.
- Review gates completed before WAIT checkpoint:
  - spec-compliance reviewer: pass
  - code-quality reviewer: pass
- Local verification run in this session:
  - =nix develop -c sh -lc 'cmake -S plugin -B build/plugin >/tmp/hyprmacs-cmake.log && cmake --build build/plugin --target hyprmacs_ipc_server_tests hyprmacs_plugin_main_tests hyprmacs_workspace_manager_tests hyprmacs_layout_applier_tests -j2 && ctest --test-dir build/plugin --output-on-failure -R "hyprmacs_(ipc_server_tests|plugin_main_tests|workspace_manager_tests|layout_applier_tests)"'=
  - result: pass.

*** DONE Session note: Task 15 review feedback intake (2026-04-22)
- User reported runtime regression at WAIT checkpoint: =just run= no longer starts the nested Hyprland session used for manual testing.
- Follow-up step added before WAIT closure to restore nested runtime launch path.
- Runtime launch wiring now uses =start-hyprland= for the nested app path in =flake.nix= (kept standard =hyprland= package; debug-override experiment was reverted).
- Verification status after this change:
  - =just run= launches nested Hyprland and emits a valid instance signature.
  - Initial =just e2e= failure during nested bootstrap (exit code =6= at first =hyprctl dispatch exec foot=) was traced to plugin startup stall.
  - Root cause: =IpcServer::start()= queried =plugin:hyprmacs:state_notify_debounce_ms= via Hyprland command socket during =PLUGIN_INIT= on compositor startup.
  - Fix applied: defer debounce-setting resolution until controller connect in =accept_loop= and keep startup default in =start()=; also removed mutex lock around =WorkspaceManager::plugin_option_int= to avoid command-socket/manager-lock inversion during runtime option reads.
  - Added a defensive non-blocking read in =WorkspaceManager::managed_workspace()= (used by tiled recalc on the compositor thread) to avoid waiting on manager mutex during command-socket dispatch paths.
  - Post-fix probes: nested startup + direct =hyprctl -j monitors= now succeeds with plugin loaded.
  - Additional fix: =manage-workspace= now focuses the managing Emacs client when available, restoring the =manage captured emacs as active class= E2E assertion path.
  - Root cause of late E2E visibility failure: successful =set-layout= snapshots did not explicitly restore hidden clients from =special:hyprmacs-hidden=, so visible clients could remain hidden because tiled recalc target iteration does not reliably include hidden-workspace targets.
  - Fix applied: on successful =set-layout= commit, run visibility reconciliation in =IpcServer= using =LayoutApplier= (=hide_client= for =hidden_clients= and =show_client= for =visible_clients=), then request recalc for geometry application.
  - Guardrail fix: =set-selected-client= now only shows selected client when committed snapshot marks it visible and provides an authoritative rectangle; otherwise it keeps the client hidden until a valid =set-layout= snapshot arrives.
  - Added/updated unit coverage in =plugin/tests/ipc_server_tests.cpp= for:
    - manage-workspace focusing managing Emacs frame
    - set-selected-client behavior with and without snapshot geometry
    - set-layout restoring visible clients from hidden workspace and preserving no-floating-geometry invariant.
  - Verification now passes:
    - targeted plugin tests: =hyprmacs_ipc_server_tests=
    - full runtime: =just e2e= pass with artifact =artifacts/e2e/20260422-184856=.

*** DONE Session note: Task 15 review-followup red E2E reproduction before fix (2026-04-22)
- User requested reproducing the reported runtime regressions as a red E2E before any further fix work.
- Expanded =hyprmacs-run-full-e2e-test= assertions in =emacs/hyprmacs.el= to cover:
  - layering/click behavior while Emacs is focused (managed client must receive click at its rectangle center)
  - managed -> floating transition after =togglefloating= (client must leave =managed_clients= on next state observation).
- Verification run in this session:
  - =just e2e= fails red as intended with assertion:
    - =floating client is removed from managed set after togglefloating=
  - artifact: =artifacts/e2e/20260422-214702=

*** DONE Session note: Task 15 review-followup floating-transition runtime fix and checkpoint closure (2026-04-22)
- User approved continuing from the Task 15 review checkpoint, so the checkpoint is now closed.
- Root cause for persistent managed-membership on floating clients:
  - plugin command-socket requests were sent with a trailing newline in =send_hypr_command_via_socket(...)=.
  - HyprCtl exact query commands (for example =j/clients= and =j/getoption=) returned =unknown request= with newline-terminated input.
  - As a result, floating-state refresh queries returned no client records, leaving managed membership stale after =togglefloating=.
- Fixes applied:
  - removed trailing newline from plugin command-socket sends in =plugin/src/plugin_main.cpp=.
  - retained request-state refresh hook in =plugin/src/ipc_server.cpp= that re-queries floating state via =WorkspaceManager::refresh_workspace_floating_state_from_query(...)= before serializing state.
  - added regression coverage in =plugin/tests/ipc_server_tests.cpp=:
    - =request-state= refresh drops floating clients from managed set, including address-shape normalization coverage.
- Additional E2E assertion hardening in =emacs/hyprmacs.el=:
  - assert compositor floating state immediately after first =togglefloating= before asserting managed-set removal.
- Verification after fix:
  - =nix develop -c sh -lc 'cmake -S plugin -B build/plugin >/tmp/hyprmacs-cmake.log && cmake --build build/plugin --target hyprmacs_ipc_server_tests hyprmacs_workspace_manager_tests -j2 && ctest --test-dir build/plugin --output-on-failure -R "hyprmacs_(ipc_server_tests|workspace_manager_tests)"'=: pass.
  - =just e2e=: pass with artifact =artifacts/e2e/20260422-222733=.

** DONE Task 16: Reproduce and fix layering + single-window geometry regressions
Purpose:
- Reproduce the reported runtime regressions in E2E first, then fix:
  - managed clients rendering behind Emacs in emacs-control mode
  - managed client geometry covering Emacs modeline in single-window layout
  - floating clients rendering behind Emacs.

Files:
- Modify =emacs/hyprmacs.el=
- Modify =emacs/hyprmacs-layout.el=
- Modify =plugin/src/plugin_main.cpp=
- Modify =plugin/tests/plugin_main_tests.cpp=
- Modify =tests/emacs/layout-tests.el=
- Modify =docs/superpowers/plans/2026-04-15-hyprmacs-iterative-implementation.org=

Steps:
*** DONE Add red E2E assertions for all three regressions
- Extend full E2E flow to assert:
  - managed client click/focus handoff while Emacs is focused in emacs-control mode
  - single-window managed geometry does not overlap Emacs modeline area
  - floating client click/focus handoff while Emacs is focused.
- Verify =just e2e= fails red with these assertions before applying fixes.

*** DONE Fix single-window managed rectangle sizing at Emacs layout source
- Ensure layout rectangle extraction uses Emacs window body area so managed clients do not cover mode line/header UI.
- Add/update Emacs layout unit tests to capture this body-area contract.

*** DONE Fix compositor layering enforcement for managed/floating vs managing Emacs
- Enforce stacking contract at runtime:
  - floating windows above managed
  - managed above managing Emacs
  - keeping emacs-control behavior intact.
- Add/update plugin-side regression tests around any new ordering helper/path.

*** DONE Session note: Task 16 layering strategy update - two floating sub-bands (2026-04-22)
- User approved the strategy: keep managing Emacs tiled/fullscreen, present managed clients as plugin-controlled floating overlays, and keep native floating above managed.
- Practical runtime contract for this step:
  - =emacs= remains tiled and anchored to workspace work area.
  - =managed= are floating windows with geometry owned by Emacs layout snapshots.
  - =floating= (native Hyprland floating clients not managed by hyprmacs) are always restacked above managed overlays.
- This unblocks Task 16 step 3 by replacing the previously blocked tiled-layering assumption.

*** DONE Session note: Task 16 compositor layering/hide race fixes and E2E green (2026-04-23)
- Root cause for =hyprctl dispatch hyprmacs:set-emacs-control-mode= timeout:
  - dispatcher callback called =publish_state_dump_for_workspace=, which routed through state-change overlay application and synchronously re-entered Hyprland command socket.
- Fix:
  - =IpcServer::publish_state_dump_for_workspace= now publishes only state dumps (debounced/immediate) without inline overlay command dispatch.
- Root cause for repeated hidden-buffer regression in floating-overlay mode:
  - late/stale workspace moves could bring a hidden managed overlay back to workspace =1= after a newer hide snapshot.
  - hidden-state cache in =LayoutApplier= could become stale relative to compositor movement ordering.
- Fixes:
  - state-change handling now enforces hidden snapshot members first, then applies z-order-only restacking.
  - added =LayoutApplier::hide_client_force(...)= and used it for state-change hidden enforcement so hidden re-assertion always dispatches even when cache says hidden.
  - reduced plugin tiled algorithm side effects so managed overlay visibility/geometry ownership stays in IPC/layout-applier paths.
- E2E assertion alignment for floating-overlay hide semantics:
  - managed-hidden assertion now accepts either hidden workspace or compositor =hidden=true=.
  - logs now include one snapshot line at hide checkpoint for diagnosis.
- Verification:
  - =nix develop -c sh -lc 'cmake --build build/plugin -j8 && ctest --test-dir build/plugin --output-on-failure'= pass.
  - =just e2e= pass with artifact =artifacts/e2e/20260423-003413=.

*** DONE Session note: Task 16 follow-up for pre-manage floating and strict geometry assertions (2026-04-23)
- User reported two additional runtime regressions after Task 16 checkpoint review:
  - managed overlay geometry/placement was wrong in single-window scenarios.
  - pre-existing floating windows (floating before manage-workspace) could still end up in hidden workspace =-98=.
- Reproduced red with test-first updates:
  - strengthened E2E single-window geometry assertion to require full rectangle alignment (x/y/width/height deltas), not only bottom-edge check.
  - added pre-manage floating E2E scenario:
    - make a client floating before manage
    - assert it stays out of =managed_clients=
    - assert it is not moved to =special:hyprmacs-hidden=
  - added plugin red unit tests:
    - =layout_applier_tests=: overlay geometry path must execute =move -> resize -> move= to keep top-left anchor stable.
    - =workspace_manager_tests=: =manage_workspace= must refresh floating state from query so clients that became floating before manage are excluded.
- Fixes:
  - restored post-resize re-anchor in =LayoutApplier::apply_floating_geometry(...)= (second =movewindowpixel= after resize).
  - added internal =WorkspaceManager::refresh_workspace_floating_state_locked(...)= and invoke it from =manage_workspace(...)= before membership reconcile.
  - kept E2E layering checks isolated by closing the dedicated pre-manage floating probe client after validating invariants, so it does not mask managed click assertions.
- Verification:
  - =nix develop -c sh -lc 'cmake --build build/plugin --target hyprmacs_layout_applier_tests hyprmacs_workspace_manager_tests -j8 && ctest --test-dir build/plugin --output-on-failure -R "hyprmacs_(layout_applier_tests|workspace_manager_tests)"'=: red before fix, green after fix.
  - =just test=: pass.
  - =just e2e=: pass with artifact =artifacts/e2e/20260423-005630=.

*** DONE Review task 16 with user
- Stop after E2E reproduces red first and then passes green with all three assertions.

*** DONE Session note: Task 16 follow-up adoption refresh split + E2E stabilization (2026-04-23)
- User approved continuing past the Task 16 WAIT checkpoint in-session, so this checkpoint is now closed.
- Added red E2E coverage for pre-manage floating -> tiled adoption without explicit =request-state=.
- Initial broad fix (workspace-wide floating refresh on all state-change notifications) reproduced adoption but introduced regressions:
  - hidden managed client lifecycle regression in =just e2e= (artifact =artifacts/e2e/20260423-173627=)
  - managed->floating transition regression in =just e2e= (artifact =artifacts/e2e/20260423-174232=)
- Final fix split floating refresh scope by caller intent:
  - =WorkspaceManager::refresh_workspace_floating_state_from_query(...)= now accepts =include_managed_clients= (default =true=).
  - =IpcServer::on_workspace_state_changed(...)= now refreshes with =include_managed_clients=false= (adopt unmanaged tiled transitions without reclassifying managed overlays).
  - explicit =request-state= path keeps full refresh (=include_managed_clients=true=) so managed->floating detection still works.
- Regression and verification status:
  - targeted plugin tests passed: =hyprmacs_workspace_manager_tests= and =hyprmacs_ipc_server_tests=
  - full suite passed: =just e2e= with artifact =artifacts/e2e/20260423-174725=.

*** DONE Session note: usability-planning kickoff (2026-04-23)
- User requested starting usability-feature planning now that the backend is mostly working.
- Next step in this session is structured usability brainstorming and design before adding new implementation tasks.

*** DONE Session note: usability must-have feature intake (2026-04-24)
- User identified day-to-day control ergonomics as the first usability-planning priority.
- Must-have usability features for this pass:
  - When a new client is managed, it opens in the currently active Emacs window.
  - A =hyprmacs= buffer is displayed in at most one Emacs window at a time; if the user displays it in a second window, the existing window should be replaced by =*scratch*=.
  - Provide a function to make a managed buffer floating, effectively unmanaging that client.
  - Provide a summon command that pulls a client from another workspace and manages it.
  - Handle focus requests from managed windows, with configurable buffer-switching behavior so users can integrate workspace packages.
- Implementation preference:
  - Keep most behavior in Emacs.
  - Add only supporting plugin APIs where Emacs cannot observe or act reliably by itself.
- Initial usability design decision:
  - When a newly managed client is assigned to the active Emacs window and that window already displays another =hyprmacs= buffer, replace the active window with the new client's buffer and hide the previous client.
  - Enforce the one-window-per-=hyprmacs=-buffer invariant globally, including ordinary Emacs display paths such as =switch-to-buffer=, =display-buffer=, and workspace/package restore flows.
  - Managed client focus requests default to: if the corresponding =hyprmacs= buffer is already visible, focus that Emacs window; otherwise display it in the currently active Emacs window.
  - Making a managed buffer floating removes/kills the corresponding =hyprmacs= buffer immediately; the client becomes a native Hyprland window.
  - Summon pulls only eligible tiled clients from other workspaces.
  - Summon is exposed as an interactive completing-read command.
  - Chosen architecture approach: Emacs-orchestrated usability policy.
    Emacs owns display/window policy, uniqueness enforcement, summon UI, and focus-request handling.
    The plugin provides only supporting APIs/events for compositor state and operations Emacs cannot perform reliably by itself.
  - Implementation must maintain nested E2E coverage in TDD style for each feature:
    write or extend failing ERT/plugin tests and the relevant failing E2E assertion first, implement the feature, then restore green tests before moving to the next feature.
- Follow-up planning artifact:
  - Implementation plan written at =docs/superpowers/plans/2026-04-24-usability-ergonomics.md=.
  - Work must continue on branch =usability-ergonomics=.
  - Do not commit usability work to =main= before explicit user approval.

Validation:
- =just test=
- =just e2e=
- Manual nested checks:
  - in emacs-control, managed windows remain visible above Emacs without requiring click-to-raise side effect.
  - in single-window Emacs layout, managed client no longer covers modeline.
  - native floating windows remain unmanaged and visibly above Emacs + managed windows.

*** Session note: Task 16 layering blocker from compositor focus/render semantics (2026-04-22)
- Task 16 single-window geometry regression is now fixed in E2E (client rectangle aligns with Emacs body area).
- Remaining failing E2E assertions are the two managed layering/click assertions while Emacs is focused.
- Investigation against Hyprland source for this runtime shows:
  - non-floating workspace render path draws the focused tiled window after other tiled windows (=renderWorkspaceWindows=, active-window last branch).
  - pointer hit-testing for tiled windows iterates compositor window list in forward order, which does not align with “focused tiled window rendered last”.
- Current consequence: with Emacs focused and tiled, managed tiled clients cannot be both visibly above Emacs and receive immediate click handoff under current approach.
- Next step requires design choice:
  - keep managed clients tiled and relax focus/visibility expectation, or
  - change managed presentation strategy (for example managed-overlay/floating treatment while preserving unmanaged native floating behavior).

* Execution Order
- Start with task 0 and task 1 only.
- Do not start task 2 until the repo builds cleanly.
- Do not start task 7 until task 6 proves hide and restore semantics.
- Treat task 8 as the boundary between prototype and core feature delivery.
- Treat task 9 and task 10 as release blockers for any serious daily-use experiment.

* Questions To Keep Open While We Build
- Which Hyprland primitive gives the cleanest hide and restore behavior for live clients.
- Whether client-attached popup surfaces need any extra handling beyond staying with their owning client.
- Whether Emacs-owned auxiliary windows can be identified robustly by PID plus window role or class information alone.
- Whether the plugin should retain a last-known layout snapshot for short transient Emacs stalls in a later version.

* Suggested First Iteration
- Execute task 0 to freeze the v1 contract.
- Execute task 1 to scaffold the build, test, and demo loop.
- Review both outputs together before touching client behavior.
